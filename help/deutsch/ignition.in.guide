@database ignitionhelp

@$VER: ignition.guide 1.0 (21.1.2004)
@author Axel Dörfler
@(c) 1998-2004 pinc Software

@wordwrap
@index index


@node main "ignition - Hilfe"
 @{b}Inhalt@{ub}

In diesem Guide finden Sie Hilfe zu folgenden Themen:

 @{b}1. Einführung@{ub}
   @{" Benutzung der Hilfe               1.1." link help}
   @{" Eingabe von Text & Formeln        1.2." link cell_input}
   @{" Interaktiver Modus                1.3." link interactive_mode}
   @{" Drag & Drop                       1.4." link drag&drop}
   @{" Anfänger-Modus                    1.5." link beginner}
#ifdef IGNITION_LITE_EDITION
   @{" Unterschiede zur Standard Edition 1.6 " link full_version}
#endif
@rem

 @{b}2. Referenz / Kontext-sensitive Hilfe@{ub}
   @{" Fenster                           2.1." link windows}
   @{" Menüs                             2.2." link menus}
   @{" Piktogrammleiste                  2.3." link iconbar}
   @{" Werkzeugleiste                    2.4." link toolbar}
   @{" Formelleiste                      2.5." link formbar}
   @{" Hilfeleiste                       2.6." link helpbar}

 @{b}3. Anhang@{ub}
   @{" Fragen & Antworten                3.1." link faq}
   @{" Fehlersuche                       3.2." link troubleshooting}
   @{" Support-Adresse                   3.3." link support}

In Kapitel 2.2. und 2.3. wird nur auf die Standardkonfiguration eingegangen; Ihre Änderungen an dieser können hier nicht berücksichtigt werden.
Sie können allerdings bei der Konfiguration sämtlichen Änderungen eigene Hilfetexte zuweisen.

Copyright ©1998-2004, pinc Software. Alle Rechte vorbehalten.
@endnode


***********************************************************************************
* 1. Einführung                                                                   *
***********************************************************************************

@node help "ignition - Hilfe benutzen"
 @{b}1.1. Benutzung der Hilfe@{ub}

Grundsätzlich wird die Hilfe über die "Help"-Taste auf Ihrer Tastatur aufgerufen - entscheidend ist dabei, was sie zu diesem Zeitpunkt unter Ihrem Mauszeiger haben.
Dabei wird im eigentlichen Dokument zwischen den einzelnen Bedienelementen unterschieden, während es für alle anderen Fenster normalerweise nur einen Eintrag gibt.
Eine Ausnahme aus dieser Regel sind Fenster mit den "Karteikarten"-Reitern, die für jede dieser Karten einen eigenen Eintrag in der Hilfe haben.

Die einzelnen Einträge sind dabei auch untereinander im Text verlinkt. Zusätzlich können manchmal auch Aktionen im Hauptprogramm hervorgerufen werden. Diese Aktionen sind, wenn vorhanden, immer durch eine kurze Linie abgesetzt, am Ende eines Eintrages zu finden.
Diese Funktionalität wird über ARexx erreicht, da sich ignition komplett darüber fernsteuern läßt. Allerdings wird als ARexx-Port immer "ignition.1" angenommen; sollten sie ignition also mehrfach gestartet haben, wird die Aktion nicht unbedingt im gerade aktuellen Programm stattfinden, auch evtl. auch gar nicht passieren, sollte der ARexx-Port gerade nicht oder nicht mehr vorhanden sein.

Die Position des Fensters auf dem Bildschirm kann übrigens auch gespeichert werden: wählen Sie dazu aus dem Hilfe-Menü "Voreinstellungen" den (einzigen) Menüpunkt "als Vorgabe speichern" aus.

@endnode


@node cell_input "ignition - Text- und Formeleingabe"
 @{b}1.2. Eingabe von Text und Formeln@{ub}

Der erste Unterschied im Vergleich mit anderen Tabellenkalkulationen wird offensichtlich, wenn man einen bereits vorhandenen Text überschreiben möchte; beginnt man in einer Zelle mit Inhalt zu tippen, wird nicht dieser bereits vorhandene Inhalt gelöscht, sondern er kann geändert werden - je nach Konfiguration der Tastatur genügt auch ein einmaliges Drücken der "Shift"-Taste (auch "Umschalt"-Taste genannt) um in die Texteingabe zu gelangen.

ignition unterscheidet bei der Eingabe grundsätzlich zwischen Texten, Werten und Formeln:

@{b}Text:@{ub} Es wird alles als Text erkannt, was nicht in die
   definierten Formatvorlagen paßt.
   Beginnt die Eingabe mit '#' wird sie immer als Text erkannt,
   auch wenn hier Zahlenwerte eingeben werden.
   Beginnt ein als Text erkannter Text mit einer Zahl (nicht
   mit '#'), hat er bei Berechnungen den Wert dieser Zahl.
   Z.B. hat der Eintrag "42 Autos" den Wert 42, obwohl keine
   passende Formatvorlage definiert ist.

@{b}Werte:@{ub} Bei jeder Eingabe wird getestet, ob sie einer der definierten
   @{"Formatvorlagen" link window_prefs_formats} entspricht. Wichtig ist, daß eine solche Eingabe
   dann auch über diese Vorlage erkannt wird. So kann die Eingabe "0:42",
   je nachdem, welche Vorlage benutzt wird, sowohl 42 Minuten als auch 42
   Sekunden bedeuten. Gültige, von den vorgegebenen Formatvorlagen erkannte,
   Eingaben wären z.B. 'Montag' oder auch nur '42'.

   Es gibt folgende grundsätzliche Typen:
     Wert    - die Eingabe wird normal übernommen
     Prozent - Eingabe in Prozent
     Datum   - in Tagen
     Zeit    - in Sekunden
     Einheit - Werte mit Einheit, z.Zt. nur andeutungsweise

   Diese Typen können beim Berechnen durcheinandergewürfelt werden, die Einheit
   ist aber immer zu berücksichtigen. Das Ergebnis wird dann wieder in der
   Formatvorlage dieser Berechnung dargestellt.
   Als Trenner von Vor- und Nachkomma-Werten wird sowohl "." als auch "," akzeptiert.

@{b}Formeln:@{ub} Beginnt die Eingabe mit einem '=' wird die Eingabe als Formel gelesen.
   Folgende Operatoren kennt ignition:
     + - * /  - Grundrechenarten
     :        - Bereich zwischen Zellen (z.B. a3:a5)
     !        - Fakultät (wird evtl. aber noch entfernt)
     > >= < <=
     = !=     - Vergleichszeichen: gleich und ungleich
     & | ~    - bitweise Verknüpfungen: und, oder, nicht
     && ||    - logische Verknüpfungen
     %        - Modulo
     ^        - Potenz

   Klein-/Großschreibung wird bei Funktionen, Namen, etc. ignoriert.
   Angefangene Funktionsnamen, Namen und Datenbanken können über die
   Tabulator-Taste vervollständigt werden; gibt es mehrere Möglichkeiten,
   werden diese durch erneutes Drücken dieser Taste durchblättert.

   Neben Operatoren und Zahlen (bislang nicht in der Exponentialschreibweise)
   können die folgenden Operanden benutzt werden:

   @{b}Zeichenfolgen@{ub}
   Alle Zeichenfolgen/Strings/Texte sind grundsätzlich in Anführungszeichen
   zu setzen, wenn sie auch als solche verwandt werden sollen. Auf Texte
   sind nicht alle Operatoren zulässig; darüberhinaus können Texte und
   Zahlen nicht gemischt werden.
   Beispiel: "Hallo "

   @{b}Bezüge@{ub}
   Endet ein Name mit einer Ziffer, wird von einem Zellbezug
   ausgegangen. Mit '$' kann bei Bezügen absolute Adressierung
   benutzt werden.
   Mit ':' gibt man bei Funktionen Bereiche an. Erwartet die
   Funktion an dieser Stelle keinen Bereich oder verwendet
   man einen Bereich innerhalb einer normalen Formel, wird
   die Summe dieses Bereiches gebildet. "=summe(a3:c6)" ent-
   spricht also "=a3:c6".
   Mit '#' leitet man einen Bezug auf eine andere Seite der
   Mappe ein, danach kann entweder der Name der Seite oder oder
   die Seitenzahl stehen. Vor dem eigentlichen Bezug folgt
   dann noch ein '#'.
   Bei der Angabe einer Seitenzahl ist zu beachten, daß sich
   diese durchaus noch ändern kann, wodurch Verweise ungültig
   werden können. Bislang wird in diesem Fall noch nicht
   gewarnt.
   Beispiel: a2, $b3, $t$42, #Seite 2#a4, #2#b3, #2#a4:#2#b6

   @{b}Funktionen@{ub}
   Öffnet sich nach einer Zeichenfolge eine Klammer geht ignition
   von einer Funktion aus.
   Eine sehr kurze Beschreibung dieser findet sich im Auswahl-
   requester bei "Wählen..." im Projektfenster.
   Die Anzahl der implementierten Funktionen ist noch nicht
   vollständig.

   @{b}Namen & Datenbanken@{ub}
   Bei Datenbanken kann auf deren Felder mittels '.' zugegriffen
   werden (z.B. personen.name, oder personen.2 für das 2. Feld).
   Falls ein Name für einen Bereich steht, ist das damit auch
   möglich.
   Bezüge bei Namen sind grundsätzlich absolut, auf ein '$' kann
   also verzichtet werden.

@endnode


@node interactive_mode "ignition - Interaktiver Modus"
 @{b}1.3. Interaktiver Modus@{ub}

ignition verfügt über zwei grundsätzliche verschiedene Betriebs-Modi:

 · im normalen Ediermodus stehen alle Funktionen zur Verfügung,
   Masken und Knöpfe können verändert und hinzugefügt werden,
   haben jedoch keine Funktion; Ereignisse werden nicht bedient.

 · im interaktiven Modus können Sie Knöpfe und andere in der Tabelle
   enthaltene Bedienelemente benutzen, sowie auch Datenbankmasken
   benutzen.
   Darüberhinaus findet eine vom Autor der Tabelle definierte
   Ereignisauswertung statt.
   Es können allerdings keine Blöcke oder Objekte markiert werden.

In der rechten Ecke der Hilfeleiste wird der aktuelle Modus durch ein "edit" bzw. "inter" für den Ediermodus bzw. den interaktiven Modus dargestellt.
Siehe auch: @{" Hilfsleiste " link helpbar}, oder @{" Piktogrammleiste " link iconbar}, in der in der Standardkonfiguration ein Knopf vorhanden ist, um in den jeweils anderen Modus zu wechseln.

Berücksichtigen Sie daß beim ersten Einladen eines Dokumentes der interaktive Modus nach Nachfrage beim Benutzer deaktiviert werden kann, auch wenn das Dokument im interaktiven Modus gespeichert wurde.
Das ist eine Sicherheitsmaßnahme, die das Öffnen unbekannter Dokumente sicherer machen soll. Bereits bekannte Dokumente werden in der Sitzungsdatei vermerkt.

@rem
#ifdef IGNITION_LITE_EDITION
@{b}Hinweis: Der interaktive Modus steht Ihnen in der "Lite" Edition von ignition nicht zur Verfügung! @{ub}
#else
@{u}    @{uu}

@{" In den interaktiven Modus wechseln " rxs "address ignition.1 interactive set"} @{" In den Editier-Modus wechseln " rxs "address ignition.1 interactive none"}
#endif
@endnode


@node drag&drop "ignition - Drag & Drop"
 @{b}1.4. Drag & Drop@{ub}

Drag&Drop bezeichnet das Aufnehmen eines Objektes, um es über einem anderen wieder fallenzulassen, um damit eine bestimmte Aktion auszulösen.
Es wird in ignition an vielen Stellen genutzt, praktisch alle Listfelder unterstützen diese Möglichkeit; in den Einstellungen für die Piktogrammleiste gibt es beispielsweise keine andere Möglichkeit, um vorhandene Befehle in die Leiste zu ziehen.

Wichtig dabei ist, daß in ignition das Herabscrollen mit aktiviertem Eintrag un Drag&Drop zur gleichen Zeit funktionieren: wählen Sie einen Eintrag aus und ziehen die Maus mit gedrückter linker Maustaste nach unten, wird nicht etwa der Eintrag verschoben, sondern der nächste Eintrag ausgewählt.
Um nun einen Eintrag wirklich zu verschieben gibt es zwei Möglichkeiten: entweder bewegen Sie die Maus ein kleines bißchen nach rechts oder links, um den Eintrag aus der Liste zu lösen, oder sie wählen den Eintrag mit zusätzlich gedrückter rechter "Alt"-Taste an.
In beiden Fällen wird nun der Eintrag über den Bildschirm gezogen, und kann durch Loslassen der Maustaste (die evtl. gedrückte "Alt"-Taste spielt nur beim Aufnehmen des Eintrages eine Rolle) an anderer Stelle wieder fallengelassen werden.
Üblicherweise wird die "Empfangsbereitschaft" des Zielortes visuell dargestellt, etwa durch eine schwarz-weiß gestrichelte Linie oder Rahmen.
@endnode

 
@node beginner "ignition - Anfänger-Modus"
 @{b}1.5. Der "Anfänger"-Modus@{ub}
 
ignition besitzt einen Anfänger-Modus, der automatisch angewählt ist, wenn Sie bei der Installation den Anfänger-Modus wählen.
Der Anfänger-Modus unterscheidet sich nur darin vom Modus für erfahrenere Anwender, daß weniger Einstellungsmöglichkeiten vorhanden sind. Die folgenden Einstellungen sind in diesem Modus nicht erreichbar:

 · Befehle
#ifndef IGNITION_LITE_EDITION
 · Menü
 · Kontext-Menü
#endif
 · Tastatur

Desweiteren werden einige Systemeinstellungen ausgeblendet.
Gespeichert wird diese Einstellung im Piktogramm von ignition selber - um sie zu deaktivieren, setzen sie das ToolType "BEGINNER" in Klammern ("(BEGINNER)").
@endnode


#ifdef IGNITION_LITE_EDITION
@node full_version "ignition - Unterschiede zur Standard Edition"
 @{b}1.6. Unterschiede zur Standard Edition@{ub}

Die vorliegende Version ist die "Lite Edition" von ignition. Zu der Standard Edition ergeben sich folgende Einschränkungen:

 · kein interaktiver Modus; Tabellen können keine Knöpfe, interne Skripte oder ähnliches benutzen
 · keine Datenbankmöglichkeit
 · die Menü-Einstellungen sind nicht verfügbar
 · einige grafische Module fehlen
 · der Turbocalc Import-Filter fehlt
 · Maximale Tabellengröße von 30 x 50 Zellen (unbeschränkt in der Standard Edition)
 · der ARexx-Port ist nur sehr eingeschränkt nutzbar (open, save, new, close, print, & quit)

Es besteht die Möglichkeit, zur "Standard Edition" zu einem ermäßigtem Preis zu updaten. Fragen Sie bitte bei Ihrem Händler nach.
@endnode
#endif


***********************************************************************************
* 2. Kontext-sensitive Hilfe                                                      *
***********************************************************************************

@node unknown "ignition - Hilfe"
Zu diesem Kontext/Thema ist keine Hilfe verfügbar.
@endnode


***********************************************************************************
* 2.1. Fenster                                                                    *
***********************************************************************************

@node windows "ignition - Hilfe zu den verschiedenen Fenstern"
 @{b}2.1. Fenster@{ub}

In diesem Kapitel finden Sie eine Auflistung aller Fenster und eine Erklärung ihrer Bedienung.

 @{b}Dokument@{ub}
   @{" Dokumentparameter ändern     " link window_document}
   @{" Dokument-Informationen       " link window_doc_info}
   @{" Drucken                      " link window_print}
   : @{" Druck-Status               " link window_printstatus}
   @{" Projektfenster               " link project}
   @{" Tabelleneigenschaften        " link window_page}
   @{" Titel setzen                 " link window_set_cell_title}
   @{" Seite einrichten             " link window_page_setup}
#ifndef IGNITION_LITE_EDITION
   @{" Skriptverwaltung             " link window_scripts}
#endif
   @{" Suchen                       " link window_search}
   @{" Suchen & Ersetzen            " link window_replace}
   @{" Vergrößerung                 " link window_zoom}

 : @{b}Zelle@{ub}
     @{" Rahmen                     " link window_frame}
     @{" Zelleigenschaften          " link window_cell}
     @{" Zellengröße                " link window_cell_size}
     @{" Zellnotizen                " link window_notes}

 : @{b}Berechnung@{ub}
     @{" Formel auswählen           " link window_formula}
     @{" Namen setzen               " link window_set_name}

 : @{b}Grafik & Diagramm-Objekte@{ub}
     @{" Objekt erstellen           " link window_insert_object}
     @{" Objektinformationen        " link window_object}
     @{" Diagramm erstellen/ändern  " link window_create_diagram}
     @{" Diagramm-Vorschau          " link window_diagram_preview}

#ifndef IGNITION_LITE_EDITION
 : @{b}Datenbank@{ub}
     @{" Datenbank definieren       " link window_database}
     @{" Filter erstellen           " link window_database_filter}
     @{" Index erstellen            " link window_database_index}
     @{" Maske erstellen            " link window_database_mask}
#endif

 @{b}Einstellungen@{ub}
   @{" Einstellungen                " link window_prefs}
   : @{" Einstellungen hinzufügen   " link window_prefs_add_prefs}

   @{" Befehle                      " link window_prefs_commands}
   : @{" Befehle definieren         " link window_prefs_define_command}
   @{" Bildschirm                   " link window_prefs_screen}
   @{" Darstellung                  " link window_prefs_display}
   @{" Datei                        " link window_prefs_file}
   @{" Formatvorlagen               " link window_prefs_formats}
#ifndef IGNITION_LITE_EDITION
   @{" Kontext-Menü                 " link window_prefs_context_menus}
   @{" Menü                         " link window_prefs_menu}
#endif
   @{" Namen definieren             " link window_prefs_names}
   @{" Palette                      " link window_prefs_palette}
   @{" Piktogrammleiste             " link window_prefs_iconbar}
   @{" System                       " link window_prefs_system}
   @{" Tabelle                      " link window_prefs_table}
   @{" Tastatur                     " link window_prefs_keyboard}
   : @{" Tastaturaufnahme           " link window_prefs_record_key}

 @{b}Diverses@{ub}
   @{" Befehl ausführen             " link window_command}
   @{" Dateityp auswählen           " link window_filetype}
   @{" Zwischenablage               " link window_clipboard}

@endnode


@node project "ignition - Projektfenster"
@toc windows
Das Projektfenster besteht aus folgenden Elementen:

 @{" Tabelle                " link proj_table}
 @{" Spalten- & Zeilentitel " link proj_anti}
 @{" Piktogrammleiste       " link iconbar}
 @{" Werkzeugleiste         " link toolbar}
 @{" Formelleiste           " link formbar}
 @{" Hilfeleiste            " link helpbar}

@endnode


@node proj_anti "ignition - Spalten- & Zeilentitel"
@toc project
Die Spalten- & Zeilentitel geben die Position und Adressierung in der Tabelle an. Spalten werden mit durchlaufenden Buchstaben identifiziert, während die Zeilen einfach durchnumeriert werden.
Eine Zelle in Spalte "a" und Zeile 5 wird mit "a5" angesprochen.
Sie können den Titeln aber auch Namen geben, unter denen dann auch die Zellen angesprochen werden können, oder sie für Datenbanken als Feldnamen benutzt werden.

Wenn Sie mit der linken Maustaste auf den Rand zwischen zwei Titeln klicken, können Sie die Größe des Feldes anpassen, während sie die Maustaste gedrückt halten.

Über das Kontextmenü kann außerdem je nach Konfiguration auch eine neue Spalte/Zeile eingefügt werden.
@endnode


@node proj_table "ignition - Tabelle"
@toc project
Dies ist die Ansicht der eigentlichen Tabelle. Klicken Sie einfach auf irgendeine Zelle, um sie zu aktivieren. Wenn Sie Text eingeben, wird dieser einfach in der aktuellen Zelle eingefügt.
Zellen und grafische Objekte haben eigene Kontext-Menüs, die mit der rechten Maustaste aufgerufen werden können.

Eine Übersicht über die anderen Elemente in diesem Fenster bekommen Sie @{" hier " link project}.
@endnode


@node proj_apen "ignition - Werkzeugleiste"
@toc project
In diesem Feld wird entweder die Standard-Schriftfarbe für dieses Dokument oder die zuletzt ausgewählte angezeigt. Drücken Sie einfach auf dieses Feld, um der aktuellen Zelle diese Farbe zuzuordnen.

Mit dem rechts danebenliegenden Knopf können Sie eine andere wählen.
Im Fenster @{" Einstellungen - Palette " link windows_prefs_palette} können Farben definiert werden.

@{u}    @{uu}

@{" Fenster «Einstellungen - Palette» öffnen " rxs "address ignition.1 prefs colors req"}
@endnode


@node proj_bpen "ignition - Werkzeugleiste"
@toc project
In diesem Feld wird entweder die Standard-Hintergrundfarbe für dieses Dokument oder die zuletzt ausgewählte angezeigt. Drücken Sie einfach auf dieses Feld, um der aktuellen Zelle diese Farbe zuzuordnen.

Mit dem rechts danebenliegenden Knopf können Sie eine andere wählen.
Im Fenster @{" Einstellungen - Palette " link windows_prefs_palette} können Farben definiert werden.

@{u}    @{uu}

@{" Fenster «Einstellungen - Palette» öffnen " rxs "address ignition.1 prefs colors req"}
@endnode


@node proj_gadget039 "ignition - Projektfenster"
@toc project
In diesem Feld wird der Name der aktuell dargestellten Seite gezeigt.

Mit dem rechts danebenliegenden Knopf können Sie die darzustellende Seite auswählen.
@endnode


@node proj_gadget040 "ignition - Projektfenster"
@toc project
Mit diesem Knopf können Sie die darzustellende Seite auswählen.

Links daneben wird der Name der gerade dargestellten Seite angezeigt.
@endnode


@node proj_gadget041 "ignition - Projektfenster"
@toc project
Wenn Sie diesen Knopf gedrückt halten, können Sie aus einer Liste von Funktionen eine an der aktuellen Zellposition einfügen.

@remark Die Auswahl dieser Liste bestimmen Sie im Fenster @{"Formel auswählen" rxs "address ignition.1 formula req"}; die dort zuletzt ausgewählte Kategorie wird benutzt.
Die Auswahl dieser Liste bestimmen Sie im Fenster @{" Formel auswählen " link window_formula}; die dort zuletzt ausgewählte Kategorie wird benutzt.
@endnode


@node proj_gadget042 "ignition - Projektfenster"
@toc project
Sie können dieses Eingabefeld benutzen, anstatt direkt in die aktuelle Zelle zu schreiben.

Bei kleinen Schriften in Zellen oder längeren Formeln in schmalen Zellen ist das möglicherweise etwas übersichtlicher.
@endnode


@node proj_gadget043 "ignition - Projektfenster"
@toc project
Wenn Sie diesen Knopf drücken, erscheint das Fenster @{" Formel auswählen " link window_formula} mit dem Sie eine Funktion in die aktuelle Zelle einfügen können.
@endnode


@node proj_gadget045 "ignition - Projektfenster"
@toc project
Diesem Piktogramm ist keine Hilfe zugewiesen.

Über @{" Einstellungen - Befehle " link windows_prefs_commands} können Sie Befehlen einen Hilfetext in einer beliebigen Hypertext-Datei zuweisen.

@{u}    @{uu}

@{" Fenster «Einstellungen - Befehle» öffnen " rxs "address ignition.1 prefs cmds req global"}
@endnode


@node proj_gadget046 "ignition - Projektfenster"
@toc project
In diesem Feld wird die aktuell ausgewählte bzw. der in die aktuellen Zelle verwendete Schrift angezeigt.

Mit dem rechts danebenliegenden Knopf können Sie eine andere Schrift auswählen.
@endnode


@node proj_gadget047 "ignition - Projektfenster"
@toc project
In diesem Eingabefeld wird die Schriftgröße der aktuellen Zelle angezeigt.

Sie können hier direkt andere Größen, auch in Millimetern, Pixeln, etc. angeben. Mit dem Knopf rechts daneben können Sie eine Größe aus einer vorgegebenen Auswahl bestimmen.
@endnode


@node proj_gadget048 "ignition - Projektfenster"
@toc project
In diesem Eingabefeld wird die aktuelle Vergrößerungsstufe der dargestellten Seite angezeigt. Sie kann hier direkt durch eine Prozentangabe verändert werden.

Es werden z.Z. Werte zwischen 25% und 1000% akzeptiert.
@endnode


@node proj_gadget049 "ignition - Projektfenster"
@toc project
Wenn Sie diesen Knopf gedrückt halten, können Sie eine Schrift für die aktuelle Zelle auswählen.

Diese wird dann im Feld links daneben angezeigt.
@endnode


@node proj_gadget050 "ignition - Projektfenster"
@toc project
Wenn Sie diesen Knopf gedrückt halten, können Sie aus einer Auswahl an vorgegebenen Schriftgrößen eine für die aktuelle Zelle auswählen.

Diese wird dann im Feld links daneben angezeigt.
@endnode


@node proj_gadget051 "ignition - Projektfenster"
@toc project
Mit diesem Knopf wird die Schrift in den ausgewählten Zellen wieder normal dargestellt.

Schriftstile wie Fett oder Kursiv werden rückgängig gemacht.
@endnode


@node proj_gadget052 "ignition - Projektfenster"
@toc project
Mit diesem Knopf wird die Schrift in den ausgewählten Zellen im Stil Fett dargestellt.
@endnode


@node proj_gadget053 "ignition - Projektfenster"
@toc project
Mit diesem Knopf wird die Schrift in den ausgewählten Zellen im Stil Kursiv dargestellt.
@endnode


@node proj_gadget054 "ignition - Projektfenster"
@toc project
Mit diesem Knopf wird die Schrift in den ausgewählten Zellen unterstrichen.
@endnode


@node proj_gadget055 "ignition - Projektfenster"
@toc project
Wenn Sie diesen Knopf drücken, können Sie die Vordergrundfarbe der ausgewählten Zellen aus einer Auswahl an vordefinierten Farben bestimmen.

Im Fenster @{" Einstellungen - Palette " link windows_prefs_palette} können Farben definiert werden.

@{u}    @{uu}

@{" Fenster «Einstellungen - Palette» öffnen " rxs "address ignition.1 prefs colors req"}
@endnode


@node proj_gadget056 "ignition - Projektfenster"
@toc project
Wenn Sie diesen Knopf drücken, können Sie die Hintergrundfarbe der ausgewählten Zellen aus einer Auswahl an vordefinierten Farben bestimmen.

Im Fenster @{" Einstellungen - Palette " link window_prefs_palette} können Farben definiert werden.

@{u}    @{uu}

@{" Fenster «Einstellungen - Palette» öffnen " rxs "address ignition.1 prefs colors req"}
@endnode


@node proj_gadget057 "ignition - Projektfenster"
@toc project
Dieser Knopf sorgt für links ausgerichteten Text in den ausgewählten Zellen.
@endnode


@node proj_gadget058 "ignition - Projektfenster"
@toc project
Dieser Knopf sorgt für zentrierten Text in den ausgewählten Zellen.
@endnode


@node proj_gadget059 "ignition - Projektfenster"
@toc project
Dieser Knopf sorgt für rechts ausgerichteten Text in den ausgewählten Zellen.
@endnode


@node proj_gadget060 "ignition - Projektfenster"
@toc project
Wenn Sie diesen Knopf drücken, können Sie aus einer Auswahl an vorgegebenen Vergrößerungsstufen die aktuelle bestimmen.
@endnode


@node proj_gadget061 "ignition - Projektfenster"
@toc project
Dieser Knopf sorgt für dafür, daß der Text in den ausgewählten Zellen am oberen Rand anliegt.
@endnode


@node proj_gadget062 "ignition - Projektfenster"
@toc project
Mit diesem Knopf wird der Text in den ausgewählten Zellen vertikal zentriert.
@endnode


@node proj_gadget063 "ignition - Projektfenster"
@toc project
Dieser Knopf sorgt für dafür, daß der Text in den ausgewählten Zellen am unteren Rand anliegt.
@endnode


  ***
 *** Misc. Windows
***

@node window_clipboard "ignition - Fenster «Zwischenablage»"
@toc windows
In diesem Fenster sehen sie die bisherigen Einträge in der Zwischenablage (Clipboard).
Sie können Einträge daraus in die Tabelle an der aktuellen Position einfügen (über den Knopf "Einfügen"), oder auch aus der Zwischenablage komplett entfernen (über den Knopf "Löschen").
@endnode

@node window_search "ignition - Fenster «Suchen»"
@toc windows
Hier können Sie nach Zellen anhand mehrer Merkmale suchen. Wählen Sie aus der Liste Merkmale aus, und bestimmen Sie, wonach genach gesucht werden soll.
Alle Merkmale der markierten Einträge (die mit einem Haken davor) müssen dabei bei einer Zelle vorhanden sein, um den Suchkriterien zu entsprechen.

Im "Optionen"-Bereich können Sie noch allgemeine Suchoptionen festlegen, die je nach ausgewählten Merkmalen angewandt werden:
Die Suchrelation gilt für die meisten Merkmale, während alle anderen nur für textbasierte Merkmale sinnvoll sind.
Die Option "mit Suchmuster" erlaubt dabei die Angabe von üblichen AmigaDOS Mustern, wie z.B. "#?" für "beliebig viel egal wovon".

Mit "Im Block" wird die Suche nach passenden Zellen dabei auf den aktuell markierten Block begrenzt, mit "Ab Zelle" wird von der aktuell ausgewählten Zelle an gesucht. Das betrifft dann alle Zellen die rechts @{b}oder@{ub} unterhalb dieser Zelle sind.

Die eingebenen Suchmerkmale werden für die nächste Suche beibehalten.
@endnode


@node window_replace "ignition - Fenster «Suchen & Ersetzen»"
@toc windows
Für die Suche der Zellen der zu ersetzenden Merkmale gibt es alle für das Fenster @{" Suchen " link window_search} beschriebenen Möglichkeiten.
Die zu suchenden Merkmale werden dabei in der linken Liste bestimmt, die zu ersetzenden in der rechten.

Wenn eine Zelle mit den gesuchten Merkmalen übereinstimmt, werden alle in der rechten Liste markierten Merkmal mit den dort bestimmten Werten ersetzt, und zwar entweder nach Bestätigung für jede Zelle oder automatisch alle, je nachdem, ob die Option "bestätigen" ausgewählt ist oder nicht.
 
Sowohl die Such- als auch die "Ersetzen durch"-Merkmale bleiben für den nächsten Vorgang erhalten. Zu bemerken ist noch, daß ein Undo/Redo eines Ersetz-Vorganges alle geänderten Zellen betrifft und nicht nur jede einzeln.
@endnode


@node window_print "ignition - Fenster «Drucken»"
@toc windows
Hier können Sie genau bestimmen, was worauf gedruckt wird. In dem Textfeld am oberen Rand des Fensters sehen Sie den aktuellen Drucker, so wie er in den Druckeinstellungen des Systems konfiguriert wurde.
An dieser Stelle können alle verfügbaren Drucker ausgewählt werden - wenn der Drucker weitergehende Einstellungen unterstützt, können diese über den "Optionen..." Knopf aufgerufen werden.

Falls Sie die Drucker konfigurieren möchten, öffnen Sie bitte die Systemeinstellungen - das geht entweder über das Einsteller-Programm in SYS:Prefs/Printer oder aber auch über den Knopf "Einstellungen".

Unter "Bereiche" können Sie genau definieren, welcher Teil ihres Dokumentes gedruckt werden soll. Dabei können Sie jeder Tabelle unterschiedliche Einstellungen zuweisen, oder sie für alle geltend machen; ist "Einstellungen gelten für alle Tabellen" nicht ausgewählt, ändern Sie jeweils nur die Einstellungen für die in der Liste angewählten Tabelle.
In dem Eingabefeld "Bereich:" können Sie den Zellbereich, der ausgedruckt werden soll, direkt angeben, z.B. "a10:d30". Die Eingabe in diesem Feld ist nur möglich, wenn der "Bereiche:" Modus auch ausgewählt ist.

Im "Optionen"-Bereich können Sie auswählen, ob der Druck asynchron ausgeführt werden soll. In diesem Fall wird der Zustand des aktuellen Dokumentes kopiert und im Hintergrund gedruckt, während Sie weiterarbeiten können. Falls Sie nur wenig freien Speicher haben, empfiehlt es sich, auf asynchronen Druck zu verzichten. In diesem Fall ist die Oberfläche von ignition so lange blockiert, bis der Druckvorgang beendet ist.
@endnode


@node window_printstatus "ignition - Fenster «Druck-Status»"
@toc windows
Dieses Fenster zeigt den Fortschritt eines aktuell andauernden Druckvorgangs an.
@endnode


@node window_filetype "ignition - Fenster «Dateityp auswählen»"
@toc windows
Hier können Sie den Dateityp auswählen, in dem das Dokument gespeichert werden soll, bzw. in dem ein vorhandenes Dokument zu öffnen versucht werden soll.
Im Zweifelsfalle verwenden Sie einfach die Voreinstellung, "ignition-Standard".
@endnode


@node window_command "ignition - Fenster «Befehl ausführen»"
@toc windows
Wählen Sie aus der Liste einen Befehl aus, oder geben Sie einen im Eingabefeld ein, der dann direkt ausgeführt werden kann.
@endnode


  ***
 *** Einstellungen
***


@node window_prefs "ignition - Fenster «Einstellungen»"
@toc windows
In diesem Fenster können Sie die verschiedenen Konfigurationsmodule in ignition erreichen und ändern. Ein Modul ist dabei eine Zusammenfassung mehrerer Einstellungsmöglichkeiten zu einem Thema.

Alle Einstellungen werden erstmal global verwaltet, und werden bei Bedarf an die offenen Projekte vererbt. Einige Einstellungen sind allerdings nur global verfügbar; diese sind in der Liste durch eine weiße Schriftfarbe zu erkennen. Beispiel für ein nur global verfügbares Modul ist z.B. @{" System " link window_prefs_system}.

Es ist allerdings auch möglich, lokale, also nur für ein bestimmtes Projekt gültige Modul-Einstellungen zu erzeugen. Dazu gibt es verschiedene Möglichkeiten:
 - entweder ziehen sie ein Modul-Objekt direkt auf den Projekt-Eintrag in der Liste,
 - drücken @{" Hinzufügen " link window_prefs_add_prefs} und wählen dort ein Modul aus, oder
 - rufen die Einstellungen eines Moduls direkt auf, z.B. über die Werkzeugleiste
   eines Projektes.

Einige lokale Module bieten darüber hinaus noch die Möglichkeit, die globalen Module nicht zu überlagern, sondern werden zusätzlich ins Projekt eingebunden - so kann ein Projekt z.B. @{" Formatvorlagen " link window_prefs_formats} sowohl vom globalen als auch vom lokalen Modul benutzen.

Der aktuell gewählte Modus (ersetzen/zusammenfügen) wird durch ein +/- nach dem Namen des Moduls in der Listen angedeutet - drücken Sie einfach auf dieses Symbol, um diese Einstellung zu ändern.

Wird ein globales Modul entfernt, wird automatisch ein neues mit der Minimal- oder Standardkonfiguration erstellt.

Zusätzlich besteht noch die Möglichkeit, bestimmte Moduleinstellungen vorübergehend in der Ablage zu zwischenzuspeichern - diese Module gehen allerdings beim Beenden des Programmes verloren.
@endnode


@node window_prefs_display "ignition - Fenster «Einstellungen - Darstellung»"
@toc windows
Hier können Sie die optische Erscheinung eines Dokumentes beeinflussen: Bedienleisten können verschoben oder ausgeschaltet werden, ein Zellraster eingestellt werden und noch einiges mehr.
Sie können hier z.B. auch den Zeichensatz der Spalten- & Zeilentitel festlegen.

Dieses Modul kann lokal benutzt werden, d.h. einem bestimmten Dokument zugewiesen werden - die Einstellungen gelten dann nur für dieses eine Projekt, ohne das andere sie übernehmen würden.
@endnode


@node window_prefs_screen "ignition - Fenster «Einstellungen - Bildschirm»"
@toc windows
In der Gruppe "Bildschirm" können Sie bestimmen, ob ignition einen eigenen Bildschirm öffnen soll, oder auf einem existierenden öffentlichen Bildschirm.
Ein eigener Bildschirm kann dabei alle Merkmale des Workbench-Bildschirms (Auflösung, Zeichensatz, ...) übernehmen - sie können aber auch alle Merkmale selber festlegen.

Unter der Überschrift "Monitor" können Sie die Größe Ihres Monitors angeben	- dabei sollte die angegebene Größe genau der Größe des sichtbaren Bildes entsprechen.
Das ermöglicht ignition die Elemente am Bildschirm in der richtigen Größe darzustellen, 1 cm auf dem Bildschirm sind dann auch 1 cm im Programm. Achten Sie also darauf, daß diese Angabe korrekt ist.

Unter "Hintergrund" läßt sich schließlich noch ein Muster als Hintergrund für den (eigenen) Bildschirm festlegen.
@endnode


@node window_prefs_palette "ignition - Fenster «Einstellungen - Palette»"
@toc windows
Hier können Sie die Bildschirmfarben sowie die in der Tabelle verfügbare Palette konfigurieren.

In der rechten Gruppe "Palette" sehen sie die gerade ausgewählte Palette (die Auswahl wird in dem Auswahlfeld direkt über der Liste festgelegt).
Sie können Farben kopieren, löschen und neue hinzufügen. Das Programm versucht automatisch, den Farben einen sinnvollen Namen zu geben, Sie können aber auch einen eigenen Namen bestimmen, der dann vom Programm nicht mehr angetastet wird.

Die Farben ändern können Sie in der linken Gruppe "Farbe", wobei das Standard Farbrad zur Definition genutzt wird. Die Regler unterhalb des Rades können entweder im RGB oder FSH Modus betrieben werden; der Farbanteil also entweder nach Rot/Grün/Blau (RGB) oder Farbton/Sättigung/Helligkeit (FSH) bestimmt werden. 

Die "Farbauswahl" ist die Palette, aus der Sie für Zellenhintergründe, und ähnliches wählen können.
@endnode


@node window_prefs_iconbar "ignition - Fenster «Einstellungen - Piktogrammleiste»"
@toc windows
Hier können Sie festlegen, welche Piktogramme (Icons) in der Piktogrammleiste angezeigt werden.
 
In der linken Liste werden alle definierten Befehle mit Piktogramm angezeigt, in der rechten die in der Leiste vorhandenen; Sie können die Befehle einfach in die andere Liste herüberziehen (siehe auch: @{" Drag&Drop " link drag&drop}). Wenn Sie Befehle von der rechten Liste in die linke ziehen, werden sie einfach von der Piktogrammleiste entfernt. Sie können die Befehle aber auch innerhalb der rechten Liste herumziehen, um ihre Reihenfolge zu ändern.
Befehle mit Piktogramm können in den @{" Befehls-Einstellungen " link window_prefs_commands} definiert werden, bzw. mit einem anderen Piktogramm versehen werden.
Mit dem Knopf "Leerfeld" wird am Ende der Leiste ein Leerfeld bzw. Abstandhalter eingefügt. Sie können aber auch diesen Knopf in die Liste an die gewünschte Position ziehen, um ein Leerfeld einzufügen.
Die Piktogrammleiste kann in den @{" Darstellung Einstellungen " link window_prefs_display} ein- und ausgeschaltet werden.

Dieses Modul kann lokal benutzt werden, d.h. einem bestimmten Dokument zugewiesen werden - die Einstellungen gelten dann nur für dieses eine Projekt, ohne das andere sie übernehmen würden.
@{u}    @{uu}

@{" Fenster «Einstellungen - Befehle» öffnen " rxs "address ignition.1 prefs cmds req global"}
@{" Fenster «Einstellungen - Darstellung» öffnen " rxs "address ignition.1 prefs display req"}
@endnode


@node window_prefs_table "ignition - Fenster «Einstellungen - Tabelle»"
@toc windows
Hier werden einige Details zur Benutzung und Berechnung von Tabellen eingestellt.

In der Gruppe "Fehlermeldungen" kann bestimmt werden, ob jeder Berechnungsfehler in einer Zelle von einem erklärendem Dialogfenster (Requester) begleitet werden soll oder nicht. In jedem Fall wird die Meldung in der Zelle selber dargestellt.
Desweiteren können unbekannte Namen als gleich Null behandelt werden - ansonsten würde eine Fehlermeldung erscheinen.

Unter "Ziehecke" können Sie das Verhalten der rechten unteren Ecke der aktuellen Zelle bestimmen. Sie können mit der Maus auf diese Ecke klicken und daraufhin einige Funktionen wie das Kopieren oder Verschieben dieser Zelle initiieren.
Hier können Sie bestimmen, was wann passieren soll. "Qualifier" meint dabei eine der Shift/Control/Alt Tasten, die dazu genutzt werden können, eine andere Funktion hervorzurufen.

Unter "Diverses" kann dann noch ausgewählt werden, ob Nullwerte "0" angezeigt werden sollen oder nicht, ob die Formeln anstelle der berechneten Ergebnisse gezeigt werden sollen, und ob bei der Eingabe eines 2-stelligen Jahres die Jahrhundertangabe automatisch vervollständigt werden soll. Geben Sie also "98" für eine Jahresangabe ein, wird daraus automatisch 1998 gemacht. Die Vervollständigung versucht dabei ein sinnvolles Datum zu berechnen, d.h es wird nicht einfach das aktuelle Jahrhundert angenommen.
Die "Zellgrößenanpassung" bezieht sich auf eine automatische Zellgrößen-Änderung bei der Eingabe eines Textes, der von der Höhe her nicht hereinpassen würde. Schreiben Sie also mit einem 14pt Zeichensatz in eine zu niedrige Zelle, wird diese automatisch so vergrößert, daß der Text paßt.
Zudem kann noch die Positionsanzeige in der Formelleiste konfiguriert werden - wenn Sie einen Block auswählen können z.B. automatisch alle Zellen addiert angezeigt werden.
Die Formelleiste kann in den @{" Darstellung Einstellungen " link window_prefs_display} ein- und ausgeschaltet werden.

@{u}    @{uu}

@{" Fenster «Einstellungen - Darstellung» öffnen " rxs "address ignition.1 prefs display req"}
@endnode


@node window_prefs_file "ignition - Fenster «Einstellungen - Datei»"
@toc windows
Unter "Pfade" legen Sie die Standard-Suchpfade in ignition fest. Wenn Sie bspw. ein neues Dokument öffnen, wird als Standardpfad der "Dokument"-Pfad im Dateirequester erscheinen.
Die Piktogramme beziehen sich dabei auf die für die @{" Befehlsdefinition " link window_prefs_define_command} verwendeten Bilder.

Darüberhinaus können Sie noch einige Optionen beim Speichern von Dokumenten bestimmen:
- @{b}Piktogramme erzeugen@{ub}
	Beim Speichern eines Dokumentes wird ein Piktogramm für die Workbench mit erzeugt
- @{b}Sicherheitskopien@{ub}
	Die vorherige Version des Dokumentes wird mit der Endung ".bak" (wie english
	"backup") gespeichert
- @{b}keine Dateiendungen@{ub}
	Standardmäßig haben die verschiedenen Dateiformate auch ihre eigenen Dateiendungen,
	z.B. benutzt das ignition Standardformat die Endung ".igs". Mit dieser Option können
	Sie verhindern, daß eine solche Endung beim Speichern eines Dokumentes automatisch
	an den gewählten Dateinamen angehängt werden.
- @{b}Auto. Speichern@{ub}
	Sie können ignition veranlassen, Ihre aktuell bearbeiteten Dokumente nach einem
	bestimmten Intervall automatisch zu speichern. Zudem haben Sie die Möglichkeit,
	sich vom Programm daran erinnern zu lassen, Ihre Arbeiten regelmäßig zu speichern.
@endnode


#if 0
@node window_prefs_printer "ignition - Fenster «Einstellungen - Drucker»"
@toc windows
(noch nicht verfügbar)
@endnode
#endif


@node window_prefs_commands "ignition - Fenster «Einstellungen - Befehle»"
@toc windows
Hier können Sie eigene Befehle definieren, die dann z.B. im Menü oder bei Tastendruck benutzt werden können. Sie können bestehende Befehle verändern oder auch löschen.
Um einen Befehl zu ändern, öffnen Sie das Fenster @{" Befehle definieren " link window_prefs_define_command} mit einem Doppelklick.

Dieses Modul kann lokal benutzt werden, d.h. einem bestimmten Dokument zugewiesen werden - die Einstellungen gelten dann nur für dieses eine Projekt, ohne das andere sie übernehmen würden.
Dieses Modul wird im @{" Anfänger-Modus " link beginner} ausgeblendet.
@endnode


@node window_prefs_define_command "ignition - Fenster «Befehle definieren»"
@toc windows
In diesem Fenster können die in ignition verwendeten Befehle definiert werden. Ein Befehl kann dabei ein ARexx-Befehl oder -Skript sein, oder auch ein DOS-Skript.

In der oberen Box kann der Name des Befehls als auch ein eventuell vorhandenes Piktogramm bestimmt. Dieses Piktogramm wird dann z.B. in der Piktogrammleiste angezeigt - um Befehle in die Piktogrammleiste zu ziehen muß dem Befehl ein Piktogramm zugeordnet sein.
Wählen Sie einen aussagekräftigen Namen - ignition unterscheidet nicht, ob es nach einem definierten Befehl oder einem internen	Befehl sucht - falls deren Namen identisch sind, wird immer der interne Befehl ausgeführt, nie der von Ihnen definierte!

Unter "Befehle" wird der eigentliche Inhalt des Befehls festgelegt. Ein Befehl kann dabei aus einer Reihe von einzelnen Befehlen bestehen, ein einzelner Befehl kann vom folgenden Typ sein:
 - @{b}intern@{ub}
	Geben Sie direkt einen der internen Befehle von ignition
	und seiner Parameter an. Das sind die gleichen Befehle,
	wie sie auch über ARexx zugänglich sind, und über die
	ignition intern komplett gesteuert wird.

 - @{b}ARexx@{ub}
	Ein ARexx-Skript wird eingeladen und ausgeführt.

 - @{b}DOS@{ub}
	Ein DOS-Skript wird eingeladen und ausgeführt.
 
@rem
#ifdef IGNITION_LITE_EDITION
Beachten Sie, daß ARexx-Skripte in der Lite Edition nicht funktionstüchtig sind.

@rem
#endif
In der Gruppe "Ausgaben" kann zum einen eine Ausgabe in eine Datei oder eine Shell definiert werden, was für externe Skripte sehr praktisch sein kann.
Der unter "Hilfetext:" angegebene Text wird zum Beispiel angezeigt, wenn der Befehl in der Piktogrammleiste sichtbar ist, und der Mauszeiger über ihm zur Ruhe kommt.

Bei "Hypertext:" kann darüberhinaus noch ein Link zu einem AmigaGuide-Dokument angegeben werden, daß beim Druck auf die "Help"-Taste angesprungen wird. Wird der Befehl im "ignition.guide" (diesem Dokument) erklärt, reicht die Angabe des Kapitelnamens, z.B. "icon_redo" (ohne Anführungszeichen). Soll ein Kapitel aus einem anderen Dokument referenziert werden, so muß der Name des Dokumentes gefolgt von einem Schrägstrich vor dem Kapitelnamen stehen, z.B. "ignition.guide/icon_redo". Das Hilfe-Dokument (im AmigaGuide-Format) muß dabei in einem von ignition erreichbarem Pfad liegen (üblicherweise "HELP:<sprache>/", also hierzulande "HELP:deutsch/").
@endnode


@node window_prefs_formats "ignition - Fenster «Einstellungen - Formatvorlagen»"
@toc windows
In diesem Fenster können Sie die Formate definieren, die ignition zur Ein- und Ausgabe von Werten benutzt. In der Liste auf der linken Seite finden Sie alle bereits definierten Formate oder Formatvorlagen mit einer Vorschau, wie dieses Format dann aussieht.
In der Gruppe "Eigenschaften" wird das Format bestimmt, so wie es auch zur Erkennung bei der Eingabe benutzt wird; die in der Gruppe "Hervorhebungen" gemachten Einstellungen wirken sich hingegen nur auf die Ausgabe aus - so werden z.B. geklammerte Werte nicht als negative Werte des Formates erkannt, und auch das Gruppentrennzeichen darf in der Eingabe nicht erscheinen.
Grundsätzlich muß erstmal der Typ des Formates festgelegt werden:

- @{b}Wert@{ub}
	Der Wert der Eingabe entspricht direkt der eingegebenen Zahl,
	es gibt keine besonderen Formatoptionen. Einzig allein die
	"0" steht für eine beliebige Zahl.

- @{b}Prozent@{ub}
	Der Wert wird zum hundertstel konvertiert, 55% entsprechen
    also einem Wert von 0,55. 

- @{b}Datum@{ub}
	Beim Datum gibt es verschiedene besondere Formatoptionen:
		#y/#Y  2 bzw. 4 stellige Jahreszahl
		#m/#M  Monat als Zahl (bei #M zweistellig mit führender 0)
		#d/#D  Tag im Monat (bei #D zweistellig mit führender 0)
		%m/%M  kurzer/langer Monatsname (Dez/Dezember)
		%w/%W  kurzer/langer Wochentag (Mo/Montag)
	Die Grundeinheit beim Datum ist der Tag; mit diesem Typ
	kann ganz normal gerechnet werden. Beim Rechnen mit Monats-
	bzw. Jahres-"Auflösung" kann die Funktion datumrechnen()
	benutzt werden.
	Berücksichtigen Sie bitte, daß das Format #y mit der Ein-
	stellung für die @{" automatische Komplettierung " link window_prefs_table} von 2-stelligen
	Jahreszahlen evtl. nicht wieder erkannt wird.

- @{b}Zeit@{ub}
	Bei der Zeit gibt es ebenfalls verschiedene Formatoptionen:
		#h/#H  Stunden (bei #H zweistellig mit führender 0)
		#m/#M  Minuten (bei #M zweistellig mit führender 0)
		#s/#S  Sekunden (bei #S zweistellig mit führender 0)
	Die Grundeinheit des Zeittyps ist die Sekunde.

- @{b}Einheit@{ub}
	Der Typ Einheit ist momentan gleichbedeutend mit dem Typ
	Wert. In einer späteren Version wird er einige Standard-
	einheiten (Längenmaße) ineinander umrechnen können; er
	kann und sollte also noch nicht zur automatischen
	Umrechnung benutzt werden (d.h. eine Formel wie:
		=text('1m'*100;"0cm")
	sollte bei definierten "0m" Format nicht benutzt werden, da
	sie später möglichweise einen falschen Wert anzeigt).

Die Priorität bestimmt die Reihenfolge, in der die Formate bei der Erkennung von Eingaben durchgegangen wird, alle Prioritäten ungleich Null werden auch in der Liste angezeigt - die Liste wird immer von unten nach oben durchsucht, wobei die Priorität nur Änderungen innerhalb eines Grundtyps bewirkt.
Die Anzahl der Nachkommastellen sollte selbsterklärend sein, bei "(auto)" werden so viele Stellen angezeigt, wie zur Repräsentierung des Werte notwendig (Nullen am Ende werden abgeschnitten), maximal jedoch 10 Stellen.
Mit der Ausrichtung kann automatisch nach Erkennung bestimmt werden, wie der Text in der Zelle ausgerichtet werden soll - das wirkt aber nur, wenn die Ausrichtung in der Zelle selber (noch) nicht gesetzt wurde - die Ausrichtung der Zelle selber kann in den Zelleigenschaften @{" geändert " link window_cell_page1} werden.

Dieses Modul kann lokal benutzt werden, d.h. einem bestimmten Dokument zugewiesen werden - die Einstellungen gelten dann nur für dieses eine Projekt, ohne das andere sie übernehmen würden.

@{u}    @{uu}

@{" Fenster «Einstellungen - Tabelle» öffnen " rxs "address ignition.1 prefs table req"}
@{" Fenster «Zelleigenschaften/Farbe & Ausrichtung» öffnen " rxs "address ignition.1 align req"}
@endnode


@node window_prefs_record_key "ignition - Fenster «Tastaturaufnahme»"
@toc windows
Drücken Sie einfach eine Taste, die dann dem im Fenster @{"Einstellungen - Tastatur" link window_prefs_keyboard} ausgewählten Befehl zugeordnet wird.

Durch drücken von "Abbruch" bzw. schließen des Fensters bleibt die vorherige Tastenkombination für diesen Befehl erhalten.
@endnode


#ifndef IGNITION_LITE_EDITION
@node window_prefs_menu "ignition - Fenster «Einstellungen - Menü»"
@toc windows
ToDo: (noch nicht verfügbar)

Dieses Modul kann lokal benutzt werden, d.h. einem bestimmten Dokument zugewiesen werden - die Einstellungen gelten dann nur für dieses eine Projekt, ohne das andere sie übernehmen würden.
Dieses Modul wird im @{" Anfänger-Modus " link beginner} ausgeblendet.
@endnode
#endif	/* IGNITION_LITE_EDITION */


@node window_prefs_keyboard "ignition - Fenster «Einstellungen - Tastatur»"
@toc windows
Hier können Sie eigene Tastaturkürzel definieren. In der linken Spalte der Liste wird der Name der Taste gezeigt "c.down" steht dabei bspw. für die "Cursor-unten" Taste. In der rechten Spalte wird der dazugehörige Befehl angezeigt.
Über den Knopf "Aufnehmen" können Sie ein Kürzel aufnehmen; sie brauchen also den Namen, den ignition für eine Taste benutzt nicht zu kennen.
In dem Auswahl-Knopf daneben kann aus der Liste der Befehle der für dieses Kürzel passende Befehl ausgewählt werden.

Darüberhinaus läßt sich noch festlegen, in welchen Momenten das Tastaturkürzel benutzt werden kann:
 - @{b}immer@{ub}
	Das Kürzel ist immer verfügbar.

 - @{b}nicht während des Edierens@{ub}
	Das Kürzel steht nicht während der Eingabe eines Zelltextes zur Verfügung.

 - @{b}nur während des Edierens@{ub}
	Das Kürzel steht nur während der Eingabe eines Zelltextes zur Verfügung
	(Eingabe in der Tabelle mit sichtbarem Cursor).

Dieses Modul kann lokal benutzt werden, d.h. einem bestimmten Dokument zugewiesen werden - die Einstellungen gelten dann nur für dieses eine Projekt, ohne das andere sie übernehmen würden.
Dieses Modul wird im @{" Anfänger-Modus " link beginner} ausgeblendet.
@endnode


#if 0
@node window_prefs_font "ignition - Fenster «Einstellungen - Zeichensatz»"
@toc windows
(noch nicht verfügbar)
@endnode
#endif


#if 0
@node window_prefs_toolbar "ignition - Fenster «Einstellungen - Werkzeugleiste»"
@toc windows
(noch nicht verfügbar)
@endnode
#endif


@node window_prefs_system "ignition - Fenster «Einstellungen - System»"
@toc windows
Hier finden Sie einige allgemeine Einstellungen und zur Zwischenablage (Clipboard).
Die Einstellungen sind über zwei Karteireiter verteilt:

   @{" Diverses       " link window_prefs_system_page0}
   @{" Zwischenablage " link window_prefs_system_page1}
 
@endnode


@node window_prefs_system_page0 "ignition - Fenster «Einstellungen - System/Diverses»"
@toc windows
Hier haben Sie die folgenden Möglichkeiten:
 
- @{b}Piktogramm auf der Workbench@{ub}
	Legt fest, ob auf der Workbench ein eigenes Piktogramm gezeigt werden
	soll während das Programm läuft, oder nicht.
	Sie können z.B. Dokumente darauf fallen lassen, woraufhin diese dann
	automatisch in ignition eingeladen werden.

- @{b}Sicherheit prüfen@{ub}
	Diese Einstellung ist in Zusammenhang mit dem interaktiven Modus wichtig;
	sie verhindert, daß Dokumente, die zum ersten Mal geöffnet werden, direkt
	im interaktiven Modus starten können, um so ein unbedenkliches Öffnen von
	unbekannten Dokumenten zu ermöglichen.
	Schalten sie diese Option nicht unbedacht ab, wenn Sie fremde Dokumente
	öffnen wollen!

- @{b}Kontext-Menüs@{ub}
	Damit können Sie Kontext-Menüs ein- und ausschalten. Als Kontext-Menüs
	werden die Menüs bezeichnet, die mit einem Rechtsklick auf ein bestimmtes
	Objekt erscheinen. Diese bieten dann je nach Kontext verschiedene Auswahl-
	möglichkeiten an.
	Es wird empfohlen, diese Einstellung aktiv zu lassen.

Die folgenden Optionen stehen im Anfänger-Modus nicht zur Wahl:
 
- @{b}Fensteraufbau@{ub}
	Je nachdem, wieviel Speicher Sie in Ihrem System haben (oder ignition
	zur Verfügung stellen wollen), können Sie hier verschieden Möglichkeiten
	ausprobieren, ignition langsamer zu machen.
	
- @{b}Sitzungen benutzen@{ub}
	Die Verwendung von Sitzungen ermöglicht ignition sich die zuletzt
	benutzten Dokumente zu merken. Dazu wird beim Beenden des Programmes
	eine bestimmte Datei aktualisiert.
    Wenn das nicht gewünscht ist, können sie es mit dieser Option unterbinden. 

#if 0
- @{b}Beim Zeichnen Puffer benutzen@{ub}
	Bestimmt, ob ignition erst in den Hintergrund zeichnen soll, um dann das
	sichtbare Bild in einem Rutsch zu aktualisieren. Das benötigt zwar mehr
	Speicher, hat aber einen deutlich angenehmeren und flickerfreien Bild-
	aufbau zur Folge.
#endif
@endnode


@node window_prefs_system_page1 "ignition - Fenster «Einstellungen - System/Zwischenablage»"
@toc windows
Bei der Zwischenablage können Sie folgende Einstellungen bestimmen:
 
- @{b}Liste hochwandern@{ub}
	Beim Einfügen wird nicht immer das zuletzt ausgeschnittene eingefügt,
	sondern die interne Liste (zu sehen über das @{" Zwischenablage-Fenster " link window_clip})
	hochgewandert. D.h. wenn Sie hintereinander eine 1, 2 und 3 kopieren
	oder ausschneiden, wird, wenn Sie daraufhin 3mal einfügen der Reihe
	nach 3, 2 und 1 eingefügt.
	In der Standardeinstellung ist die Option deaktiviert.

- @{b}Clipboard-Unit@{ub}
	Falls Sie für ignition standardmäßig eine andere Zwischenablage (engl.
	Clipboard) benutzen wollen, können Sie diese über die "Unit" anwählen.
	AmigaOS unterstützt 256 (0-255) verschiedene Zwischenablagen, normaler-
	weise wird jedoch nur die erste (0) benutzt.
	Für die Zusammenarbeit mit anderen Programmen wird empfohlen, diese
	Einstellung auch auf "0" zu belassen.

@endnode


@node window_prefs_names "ignition - Fenster «Einstellungen - Namen»"
@toc windows
In diesem Fenster können Sie Namen definieren. Namen können bei Berechnungen genutzt werden, und sowohl als wiederkehrende Berechnungen vereinfachen, als auch als feststehende Konstanten benutzt werden.
So ist z.B. "pi" als Konstante definiert.
Namen sind ähnlich Variablen wie sie in Programmiersprachen benutzt werden, werden aber jeweils erst bei Benutzung ausgewertet.

Um einen neuen Namen zu definieren klicken Sie erst auf "Neu" (unter der Liste der vorhandenen Namen), und wählen dann den Namen, der wie er in Formeln verwendet werden soll.
Darüberhinaus gibt es verschiedene Typen, die ein Name haben kann:
- @{b}standard@{ub}
	Ein solcher Name kann jede Art von Formel beinhalten.
- @{b}Bezug@{ub}
	bezieht sich auf eine bestimmte Zelle, die direkt über diesen Namen angesprochen werden kann.
	Der Name wird dann auch im Positionsfeld für die aktuelle Zelle angezeigt.
- @{b}Suchkriterium@{ub}
	hat immer die aktuelle Zelle als Bezug, und kann z.B. als Filter für die Datenbank-Funktionen
	wie dbanzahl() benutzt werden. Ein mögliches Kriterium wäre z.B. "aktuelle() > 100".

Dieses Modul kann lokal benutzt werden, d.h. einem bestimmten Dokument zugewiesen werden - die Einstellungen gelten dann nur für dieses eine Projekt, ohne das andere sie übernehmen würden.

Es werden nur die Namen gezeigt, die dieses Modul definiert - werden lokale Einstellungen genutzt, und diese zusätzlich zu den globalen genutzt (erkennbar an dem Plus-Symbol neben dem Modulnamen im @{" Einstellungen " link window_prefs} Fenster), erscheinen nur die zusätzlich lokal definierten Namen, nicht aber die global vorhandenen.

@{u}    @{uu}

@{" Fenster «Einstellungen» öffnen " rxs "address ignition.1 prefs req"}
@endnode
	
 
#ifndef IGNITION_LITE_EDITION
@node window_prefs_context_menus "ignition - Fenster «Einstellungen - Kontext-Menü»"
@toc windows
ToDo: (noch nicht verfügbar)

Dieses Modul kann lokal benutzt werden, d.h. einem bestimmten Dokument zugewiesen werden - die Einstellungen gelten dann nur für dieses eine Projekt, ohne das andere sie übernehmen würden.
Dieses Modul wird im @{" Anfänger-Modus " link beginner} ausgeblendet.
@endnode
#endif	/* IGNITION_LITE_EDITION */


@node window_prefs_add_prefs "ignition - Fenster «Einstellungen hinzufügen»"
@toc windows
Dieses Fenster kann nur innerhalb der lokalen Einstellungen erreicht werden. Sie können damit den lokalen Einstellungen eines Dokumentes verschiedene Einstellungen-Module hinzufügen.
Da nicht alle Einstellungen lokal angewandt werden können, stehen in der Liste nur die Module zur Auswahl, die diese Möglichkeit tatsächlich bieten.
Im Gegensatz zum Hereinziehen anderer Einstellungen in das Projekt werden die neuen Module mit den Standard-Eigenschaften versehen.
@endnode


  ***
 ***  Map
***
	
  
@node window_page "ignition - Fenster «Tabelleneigenschaften»"
@toc windows
In diesem Fenster können Sie die Einstellungen zu einer Tabelle oder Seite treffen. Sie können den Namen ändern, in der die Tabelle im Dokument erscheint, und die voreingestellten Vorder- und Hintergrundfarben ändern.
Die besonderen Schutzmerkmale stehen noch nicht zur Verfügung.
#if 0
In diesem Fenster können Sie die Einstellungen zu einer Tabelle oder Seite treffen. Sie können den Namen ändern, in der die Tabelle im Dokument erscheint, die voreingestellten Vorder- und Hintergrundfarben ändern, sowie auch besondere Schutzmerkmale definieren.
Letztere werden evtl. durch einen aktivierten Dokumentenschutz überlagert.
Die folgenden Möglichkeiten stehen zur Wahl:
  - @{b}keine@{ub}
	Die Seite kann ganz normal editiert werden.

  - @{b}unveränderlich@{ub}
	Die Seite ist sichtbar, ihr Inhalt kann aber nicht verändert werden.

  - @{b}unsichtbar@{ub}
	Die Seite wird im Dokument nicht angezeigt, kann aber nach wie vor
	von anderen Seiten referenziert werden.
#endif
   
@endnode


@node window_document "ignition - Fenster «Dokumentparameter ändern»"
@toc windows
Hier können einige dokumentweite Eigenschaften getroffen werden.

In der Gruppe "Eigenschaften" stehen folgende Möglichkeiten zur Wahl:
#ifndef IGNITION_LITE_EDITION
 - @{b}interaktiver Modus@{ub}
	Schaltet das Dokument in den @{" interaktiven Modus " link interactive_mode}.
@rem
#endif
 - @{b}Notizen zeigen@{ub}
	Zellnotizen werden angezeigt; über den Zellen mit Notiz öffnet sich dann
	ein Rahmen mit der Notiz.

 - @{b}Fenstergröße sichern@{ub}
	Die Fenstergröße des Dokumentes wird im Projekt gespeichert. Das kann
	für spezielle Anwendungen benutzt werden.

 - @{b}unveränderlich@{ub}
	Das gesamte Dokument kann nicht verändert werden.

 - @{b}versteckte Zellen zeigen@{ub}
	Zellen mit dem Sicherheitsmerkmal "Inhalt verstecken" werden
	dennoch angezeigt.

@rem
#ifdef IGNITION_LITE_EDITION
Die Option "interaktiver Modus" ist nur in der @{" Standard-Version " link full_version} verfügbar.
@rem
#endif
Unter "Schutz" können Sie Paßwörter für die Einhaltung der Schutzmerkmale bestimmen. In Falle des Schutzes des gesamten Dokumentes kann das Dokument ohne Angabe eines Paßwortes nicht mehr geöffnet werden.

@rem
#ifndef IGNITION_LITE_EDITION
In der Gruppe "Ereignisse" können Sie Aktionen zu verschiedenen Ereignissen definieren. Bei Eintritt des Ereignisses wird der angegebene Befehl ausgeführt. Ereignisse werden nur im @{" interaktiven Modus " link interactive_mode} ausgewertet.
#else
Die Ereignisdefinition ist nur im interaktiven Modus interessant, und steht daher ebenfalls nur in der Standard-Version zur Verfügung.
#endif
@endnode


@node window_page_setup "ignition - Fenster «Seite einrichten»"
@toc windows
Hier können Sie die Seitengröße als auch Papierränder definieren.
Bei dem Punkt "Begrenzung:" kann bestimmt werden, wie ignition die Papiergrenzen sichtbar machen soll:
 - @{b}keine@{ub}
	Die Seitengrenzen werden in keiner Weise im Dokument sichtbar gemacht. Das ist die Standard-Einstellung.

 - @{b}markiert@{ub}
	Die Grenzen werden markiert, d.h. mit einer Linie mit schraffiertem Rand kenntlich gemacht.

 - @{b}fest@{ub}
	Der Bereich außerhalb der Seitengrenzen wird lediglich schraffiert dargestellt.
@endnode


@node window_doc_info "ignition - Fenster «Dokument-Informationen»"
@toc windows
Hier können Sie dokumentspezifische Informationen und Bemerkungen angeben. Diese werden mit dem Dokument gespeichert.
@endnode


@node window_zoom "ignition - Fenster «Vergrößerung»"
@toc windows
Wählen Sie eine Vergrößerungsstufe für das aktuelle Dokument aus.
@endnode
								
  
#ifndef IGNITION_LITE_EDITION
@node window_scripts "ignition - Fenster «Skript-Verwaltung»"
@toc windows
ToDo: (noch nicht verfügbar)
@endnode
#endif	/* IGNITION_LITE_EDITION */


@node window_set_cell_title "ignition - Fenster «Titel setzen»"
@toc windows
Hier können Sie einen Titel für eine Spalte oder Reihe festlegen. Dieser Titel ist nicht nur zur besseren Orientierung in der Tabelle gedacht, er wird z.B. bei der Erstellung von Datenbanken als Feldname übernommen.
@endnode


  ***
 *** Objects
***

@node window_insert_object "ignition - Fenster «Objekt einfügen»"
@toc windows
In diesem Fenster wählen Sie ein Objekt aus, das Sie in die aktuelle Tabelle einfügen können. Falls Sie gleich mehrere Objekte hintereinander einfügen möchten, können Sie die Option "Fenster offen halten" aktivieren - in diesem Fall müssen Sie das Fenster selber schließen, wenn Sie fertig sind.

Haben Sie eine bestimmte Objekt-Klasse ausgewählt, spannen Sie einen Rahmen in der aktuellen Tabelle auf, in dem das Objekt dann erstellt wird. Bei einigen Objekten müssen Sie auch nur den Anfangs- und Endpunkt markieren (wie z.B. der Linie), oder auch noch einige Punkte zwischendurch (Polygon).
@endnode


@node window_object "ignition - Fenster «Objekteigenschaften»"
@toc windows
Dieses Fenster zeigt Eigenschaften zum gewünschen Objekt. Unter "Position" können Sie exakt die Position und Größe des Objektes festlegen.
Alle anderen Eigenschaften sind abhängig von der Objektklasse.

Ist das Objekt ein Diagramm kann über den Knopf "Diagrammeinstellungen..." das Fenster @{" Diagramm ändern " link window_create_diagram} aufgerufen werden.
@endnode


@node window_create_diagram "ignition - Fenster «Diagramm erstellen/ändern»"
@toc windows
Hier können Sie je nach Kontext ein neues Diagramm erstellen, oder die Eigenschaften eines bereits vorhandenen ändern.
Die Einstellungen sind über mehrere Karteireiter verteilt:

   @{" Inhalt         " link window_create_diagram_page0}
   @{" Diagrammtyp    " link window_create_diagram_page1}
   @{" Darstellung    " link window_create_diagram_page2}
   @{" Werte          " link window_create_diagram_page3}
   @{" Achsen         " link window_create_diagram_page4}

Wird ein neues Diagramm erstellt, so finden sich im unteren Bereich die Knöpfe "Ok", "Vorschau..." und "Abbrechen". Da das Diagramm zu dem Zeitpunkt noch nicht Teil der Tabelle ist, kann mit "Vorschau" ein kleines Fenster geöffnet werden, in dem eine Vorschau auf das Diagramm gezeigt wird. Wird ein bereits vorhandenes Diagramm geändert, werden die Änderungen sofort vorgenommen, und können direkt in der Tabelle begutachtet werden.
@endnode


@node window_create_diagram_page0 "ignition - Fenster «Diagramm erstellen/ändern - Inhalt»"
@toc window_create_diagram
Hier wird festgelegt, woher das Diagramm seine Daten bekommt, und wie sie ausgewertet werden. Außerdem kann der Name des Diagrammes festgelegt werden.

Unter "Bereich:" kann der Zellbereich (wie üblich in der Form "a3:b5") angegeben werden - der Knopf "vom Block" füllt den Bereich mit der Position des momentan selektierten Zellbereiches. Sind während der Erstellung des Diagrammes bereits Zellen ausgewählt, werden diese automatisch im Bereich übernommen.

Werte "vertikal" oder "horizontal" bestimmt, in welcher Reihenfolge die Werte benutzt werden sollen. Einige Diagrammtypen unterstützen auch mehrreihige Daten; beim Balkendiagramm z.B. werden für jede Datenreihe eigene Balken angezeigt, und die hier getroffene Einstellung bestimmt, ob die Datenreihen Reihen sind oder eigentlich doch Spalten.

Unter "Seite:" kann schließlich noch bestimmt werden, aus welcher Tabelle die Daten geholt werden sollen - ein Diagramm kann sich selbstverständlich auch auf Daten aus einer anderen Tabelle beziehen, allerdings muß sie im gleichen Dokument liegen.	Voreingestellt ist dabei immer die aktuell dargestellte Tabelle.
@endnode


@node window_create_diagram_page1 "ignition - Fenster «Diagramm erstellen/ändern - Diagrammtyp»"
@toc window_create_diagram
Hier können Sie aus der Liste der verfügbaren Diagrammtypen wählen. Ein einfacher Klick wählt den Typ aus und ändert das Diagramm dementsprechend.

Beachten Sie, daß einige Einstellungen vom jeweiligen Diagrammtyp verwaltet werden, und daher bei einem Wechsel des Typs verlorengehen!
@endnode


@node window_create_diagram_page2 "ignition - Fenster «Diagramm erstellen/ändern - Darstellung»"
@toc window_create_diagram
Hier werden spezielle und für jeden Diagrammtyp unterschiedliche Möglichkeiten zur Änderung der Darstellung des Diagrammes aufgezeigt.
Z.Zt. bei fast allen Diagrammtypen vorhandene Einstellungen sind z.B.:
																						
 - @{b}Fläche durch einen Rahmen begrenzen@{ub}
	Alle Flächen des Diagrammes bekommen einen schwarzen Rahmen.

 - @{b}Pseudo 3D@{ub}
	Das Diagramm wird in der Parallelperspektive gezeichnet, was
	einen 3D Effekt hervorruft.

Probieren Sie ansonsten die Einstellungen einfach aus, um zu sehen, was genau sie bewirken.
@endnode


@node window_create_diagram_page3 "ignition - Fenster «Diagramm erstellen/ändern - Werte»"
@toc window_create_diagram
Hier werden alle Werte aus den Zellen in Reihen gruppiert angezeigt. Sie können, je nach Fähigkeiten des Diagrammtyps, die Farbe des jeweiligen Wertes ändern oder den Wert in irgendeiner Weise hervorheben (wie, ist wieder Sache des Diagrammtyps).

Beachten Sie, daß diese Einstellungen beim Wechsel des Diagrammtyps verlorengehen!
@endnode


@node window_create_diagram_page4 "ignition - Fenster «Diagramm erstellen/ändern - Achsen»"
@toc window_create_diagram
Einige Diagrammtypen benutzen eine Achsendarstellung. In diesem Fall können Sie hier das Aussehen der Achsen verändern.

In der Gruppe "Beschriftung" kann die Achsenbeschriftung ein- und ausgeschaltet, ihre Schriftart und -größe bestimmt und ihre Farbe verändert werden.

Unter "Hintergrund" können Eigenschaften des Hintergrunds des Diagrammes, die von den Achsen aufgespannte Fläche, bestimmt werden. In der Einstellung "transparent" werden die hinter dem Diagramm gelegenen Objekte/Zellen sichtbar. Sie können zudem auch noch ein Raster auf den Hintergrund legen, der dem Abstand der Beschriftungsskala entspricht.
@endnode


@node window_diagram_preview "ignition - Fenster «Diagramm-Vorschau»"
@toc windows
In diesem Fenster wird eine Vorschau des zu erstellenden Diagramms gezeigt.
Wird im @{" Diagrammfenster " link window_create_diagram} bestätigt, wird dieses Diagramm in die aktuelle Seite eingefügt.
@endnode


#if 0
@node window_object_list "ignition - Fenster «Objektliste»"
@toc windows
(noch nicht verfügbar)
@endnode
#endif


  ***
 *** Cell Properties
***

@node window_cell "ignition - Fenster «Zelleigenschaften»"
@toc windows
Hier können Sie alle möglichen Eigenschaften einer Zelle bestimmen.
Die Einstellungen sind über mehrere Karteireiter verteilt:

   @{" Format              " link window_cell_page0}
   @{" Farbe & Ausrichtung " link window_cell_page1}
   @{" Schrift             " link window_cell_page2}
   @{" Diverses            " link window_cell_page3} (Zellschutz)

Allgemein bedeutet die Einstellung "Übernehmen", daß die aktuelle Eigenschaft der Zelle nicht geändert wird.

Beachten Sie auch, daß alle Änderungen erst vorgenommen werden, wenn einer der Knöpfe "Zuweisen" oder "Ok" gedrückt werden - dabei werden dann auch alle Änderungen übernommen, also auch die der momentan nicht sichtbaren Karteireiter.
Daher sind alle Eigenschaften beim Öffnen des Fensters auf "Übernehmen" geschaltet, können aber per Knopfdruck auch die Einstellungen der gerade ausgewählten Zelle übernehmen.
"Zuweisen" läßt dabei das Fenster offen, und ermöglicht so auf einfache Weise das Anwenden gleicher Einstellungen auf verschiedene Zellen.

Die Einstellungen werden immer auf die momentan ausgewählten Zellen angewandt, also nicht unbedingt auf die zum Zeitpunkt des Öffnen des Fensters ausgewählten.
@endnode


@node window_cell_page0 "ignition - Fenster «Zelleigenschaften/Format»"
@toc window_cell
Hier können Sie eines der in @{" Einstellungen - Formate " link window_prefs_formats} gemachten Formate einer Zelle zuweisen.
Die im Format angegebenen Eigenschaften werden dann z.T. übernommen. Davon abweichend kann noch die Anzahl der Nachkommastellen bestimmt werden.

@{u}    @{uu}

@{" Fenster «Einstellungen - Formate» öffnen " rxs "address ignition.1 prefs format global req"}
@endnode


@node window_cell_page1 "ignition - Fenster «Zelleigenschaften/Farbe & Ausrichtung»"
@toc window_cell
In der Gruppe "Farbgebung" wird die Vorder- (also Text-) und Hintergrundfarbe einer Zelle bestimmt. Die Auswahl wird nur angewandt, wenn die jeweilige Option markiert wurde.
Es kann aus der in @{" Einstellungen - Palette " link window_prefs_palette} definierten Liste ausgewählt werden.
																																											  
Analog dazu wird in der Gruppe "Muster" ein Zellmuster bestimmt, der erste PopUp-Knopf bestimmt dabei das Muster, der zweite die Farbe des Musters.

Unter "Ausrichtung" kann die Ausrichtung des Zellinhaltes innerhalb der Zelle bestimmt werden. Von Erwähnung ist noch die Option "Löschen" bei der horizontalen Ausrichtung: damit können Sie die aktuelle Einstellung löschen, und die des Formates der Zelle wird wieder wirksam.

@{u}    @{uu}

@{" Fenster «Einstellungen - Palette» öffnen " rxs "address ignition.1 prefs colors req"}
@endnode


@node window_cell_page2 "ignition - Fenster «Zelleigenschaften/Schrift»"
@toc window_cell
Hier können Sie die Schriftart und -Eigenschaften einer Zelle bestimmen.

In der Gruppe "Zeichensatz" kann die Schriftfamilie und Größe der Schrift bestimmt werden.

Unter "Stil" können verschiedene Merkmale aktiviert werden - gibt es eine Schrift für das spezielle Merkmal in der aktuellen Familie, wird diese automatisch ausgwählt. Wenn nicht, wird das Merkmal von der ähnlichsten Schrift ausgehend berechnet.

Unter "Spezial" steht momentan nur die Möglichkeit zur Wahl, die Schrift ein wenig zu neigen, positive Werte nach rechts, negative nach links.

In der Gruppe "Typografie" können dann noch Kerning und Laufweite eingestellt werden. Ersteres bestimmt den Abstand zwischen speziellen Pärchen, wie z.B. "Te" - ohne Kerning wird einfach die normale Zeichenbreite benutzt, bei den verschiedenen Kerning-Einstellungen (die aber oft identsich sind) werden die Abstände den Zeichen angepaßt; im Beispiel rückt das "e" näher an das "T" heran. Dabei hat "Design" üblicherweise einen kleineren Abstand als "Text".
Die Laufweite bestimmt den generellen Abstand der Zeichen und wird zum normalen Zeichenabstand (ob mit oder ohne Kerning) einfach dazugezählt bzw. davon abgezogen.
@endnode


@node window_cell_page3 "ignition - Fenster «Zelleigenschaften/Diverses»"
@toc window_cell
In der Gruppe "Zellschutz" können Sie verschiedene Schutzmerkmale definieren:
 - @{b}Übernehmen@{ub}
	Die aktuelle Sicherheitseinstellung der Zelle(n) wird nicht verändert.

 - @{b}keine Beschränkungen@{ub}
	Keinerlei Beschränkungen; dies ist der normale Modus.

 - @{b}nicht edierbar@{ub}
	Die Zelle kann nicht verändert werden.

 - @{b}Formel verstecken@{ub}
	Die Formel zur Berechnung der Zelle wird nicht angezeigt, und kann
	natürlich auch nicht verändert werden.

 - @{b}Inhalt verstecken@{ub}
	Der Zellinhalt wird komplett versteckt und kann auch nicht geändert
	werden.
 
@endnode


@node window_frame "ignition - Fenster «Rahmen»"
@toc windows
In diesem Fenster können Sie für die bei Bestätigung selektierten Zellen einen Rahmen setzen, der um die Zelle herum dargestellt wird. So ein Rahmen kann sowohl unterschiedliche Dicken als auch Farben haben (auch für jede Seite der Zelle unterschiedlich).

In der Gruppe "Vorgaben" können Sie unter der Vorauswahl einen geeigneten Rahmen heraussuchen - ist die Option "nur hinzufügen" angewählt, dann werden bereits vorhandene Rahmen nicht gelöscht, sondern nur die tatsächlich geänderten Seiten verändert. Eine Ausnahme davon ist der Vorgaberahmen in der linken oberen Ecke - der löscht immer alle Seiten des Rahmens, unabhängig von der Option "nur hinzufügen".
Ein Doppelklick auf eine dieser Vorgaben führt die Änderungen umgehend durch, das Fenster wird daber aber offen gelassen.

In dem Kasten unter dieser Gruppe können Sie zum einen die Auswirkungen der vorherigen Auswahl für jede Seite einzeln betrachten, zum anderen aber auch die vorgenommenen Änderungen genauer kontrollieren. Es werden nur solche Änderungen übernommen, bei denen die Option "übernehmen" aktiviert ist, alle anderen gehen beim Verlassen des Fensters verloren. Durch Druck auf Wechselknopf (auch "Cycle-Gadget" genannt), schalten Sie zwischen den verschiedenen Seiten des Rahmens durch.

Im unteren Bereich sorgt nun "Rahmen von aktueller Zelle übernehmen" dazu, daß der obige Kasten mit dem Rahmen der aktuellen Zelle gefüllt wird. Wenn die Option "ganzen Block umrahmen" ausgewählt ist, wird nicht jeder Zelle der Rahmen zugewiesen, sondern der Block praktisch als eine Zelle aufgefaßt, der der Rahmen zugewiesen wird.

Wie in anderen Fenstern auch übernimmt "Ok" die Änderungen und schließt das Fenster, "Zuweisen" übernimmt die Änderungen ebenfalls, läßt aber das Fenster offen, während "Abbrechen" das Fenster schließt, aber keine Änderungen durchführt.

@endnode


@node window_cell_size "ignition - Fenster «Zellengröße»"
@toc windows
Hiermit können Sie die Größe der Zellen sehr genau bestimmen. Sowohl bei der Breite als auch der Höhe gibt es die folgenden Möglichkeiten:
 - @{b}nicht ändern@{ub}
	Die Größe wird nicht geändert.

 - @{b}exakt@{ub}
	Ändert die Größe der ausgewählten Zellen auf exakt diesen Wert.
	Beachten Sie, daß alle Zellen einer Spalte immer die gleiche
	Breite und alle Zellen einer Reihe immer die gleiche Höhe haben.

 - @{b}mindest@{ub}
	Ist eigentlich nur sinnvoll, wenn mehr als eine Zelle ausgewählt ist;
	es vergrößert alle Zellen, die kleiner sind, als die angegebene
    Mindestgröße.

 - @{b}optimal@{ub}
	Stellt die optimale Zellgröße ein, so daß der Inhalt komplett in der
	Zelle enthalten ist (also bei der Breite nach der Länge des Textes,
	und bei der Höhe nach der Schriftgröße).

Die Änderungen werden auf die momentan angewählten Zellen angewandt, also nicht unbedingt auf die zum Zeitpunkt des Öffnen des Fensters angewählten Zellen.

Eine einfach Möglichkeit zur Änderung der Zellgröße ist, mit der Maus zwischen einen Spalten- oder Zeilentitel zu klicken, und die Zellen dann einfach auf die richtige Größe zu ziehen.
@endnode


@node window_notes "ignition - Fenster «Zellnotizen»"
@toc windows
Sie können jeder Zelle eine Notiz zuweisen, die dann gezeigt wird, wenn der Mauszeiger über der Zelle zur Ruhe kommt. Die Notiz kann mehrere Zeilen haben, Sie können die gleiche Notiz auch mehreren Zellen zuweisen, und sie auch einfach wieder löschen.

Die Änderungen werden auf die momentan angewählten Zellen angewandt, also nicht unbedingt auf die zum Zeitpunkt des Öffnen des Fensters angewählten Zellen.
@endnode


  ***
 *** Database
***

#ifndef IGNITION_LITE_EDITION
@node window_database_index "ignition - Fenster «Index erstellen»"
@toc windows
(noch nicht verfügbar)
@endnode


@node window_database "ignition - Fenster «Datenbank definieren»"
@toc windows
(noch nicht verfügbar)
@endnode


@node window_database_mask "ignition - Fenster «Maske erstellen»"
@toc windows
(noch nicht verfügbar)
@endnode


@node window_database_filter "ignition - Fenster «Filter erstellen»"
@toc windows
(noch nicht verfügbar)
@endnode
#endif	/* IGNITION_LITE_EDITION */


  ***
 *** Calculation
***

@node window_formula "ignition - Fenster «Formel auswählen»"
@toc windows
Hier können Sie aus einer nach Themengebieten sortierten Liste eine Formel auswählen, die in die aktuelle Zelle an der aktuellen Cursor-Position eingefügt wird.
Zu jeder Funktion wird zudem eine kurze Erklärung gegeben.
@endnode


@node window_set_name "ignition - Fenster «Namen setzen»"
@toc windows
Hier können Sie einen Namen für eine Zelle bestimmen. Dieser wird auch in der Positionsanzeige dargestellt, wenn die Zelle aktuell ausgewählt ist.
Die Zelle ist in Berechnungen über diesen Namen ansprechbar.
@endnode

***

@node windowXXX "ignition - Fenster «»"
@toc windows
(noch nicht verfügbar)
@endnode


***********************************************************************************
* 2.2. Menüs                                                                      *
***********************************************************************************

@node menus "ignition - Menüs"
 @{b}2.2. Menüs@{ub}

Hier finden Sie eine Erklärung der einzelnen Menüpunkte in der Standardkonfiguration.

   @{" Projekt       " link menu_project}
   @{" Bearbeiten    " link menu_edit}
   @{" Ansicht       " link menu_view}
   @{" Objekt        " link menu_object}
   @{" Einstellungen " link menu_preferences}

@endnode

 
@node menu_project "ignition - Menüs: Projekt"
 @{b}2.2. Menüs@{ub}

 @{b}Projekt@{ub}
 @{" Neu                   (N)" link item_new}

 @{" laden...              (L)" link item_load}
 @{" öffnen...             (O)" link item_open}

 @{" speichern             (S)" link item_save}
 @{" speichern als...      (A)" link item_save_as}
 @{" schließen                " link item_close}

 @{" drucken               (P)" link window_print}

 @{" Über                  (?)" link item_about}
 @{" beenden               (Q)" link item_quit}

@endnode


@node item_new "ignition - Menüs: Projekt/Neu"
Damit wird ein neues Dokument geöffnet.
@endnode


@node item_load "ignition - Menüs: Projekt/öffnen..."
Es erscheint das Standard-Dateiauswahlfenster, in dem sie ein neues Dokument laden können.
Für das neue Dokument wird in jedem Fall ein neues Fenster geöffnet.
@endnode


@node item_open "ignition - Menüs: Projekt/laden..."
Es erscheint das Standard-Dateiauswahlfenster, in dem sie ein neues Dokument laden können.
Das Dokument wird dabei im Fenster des momentan aktuellen Projektes geladen.
Änderungen bei diesem gehen aber nicht verloren - falls Änderungen zur gespeicherten Version des Dokumentes existieren, wird nochmal nachgefragt, ob es gespeichert werden soll, bevor es geschlossen wird.
@endnode


@node item_save "ignition - Menüs: Projekt/Speichern"
Speichert das aktuelle Dokument unter seinem Namen ab. Falls das Dokument noch nie gespeichert wurde, wird ein Dateiauswahlfenster geöffnet.
@endnode


@node item_save_as "ignition - Menüs: Projekt/Speichern als..."
Öffnet ein Dateiauswahlfenster in dem Sie festlegen können, wo das aktuelle Dokument gespeichert wird.
Darüberhinaus können Sie auch noch den Dateityp des Dokumentes bestimmen.
@endnode


@node item_close "ignition - Menüs: Projekt/schließen"
Schließt das aktuelle Dokument. Wenn das Fenster des Dokumentes das letzte offene Programmfenster ist, wird zudem das Programm beendet.
Falls Änderungen gegenüber der gespeicherten Version vorliegen, findet noch eine Sicherheitsabfrage statt.
@endnode


@node item_about "ignition - Menüs: Projekt/über"
Zeigt das ignition "Programminformationen" Fenster, in dem Sie die auch die Versionsnummer ablesen können.
@endnode


@node item_quit "ignition - Menüs: Projekt/beenden"
Beendet das Programm. Falls geänderte Dokumente geöffnet sind, besteht die Möglichkeit, diese noch abzuspeichern.
Bei geänderten Programmeinstellungen wird ebenfalls noch nachgefragt, ob diese gespeichert werden sollen.
@rem
@rem Diese Art der Rückfragen lassen sich unter den @{" System Einstellungen " link window_prefs_system} abschalten.
@rem
@rem @{u}    @{uu}
@rem
@rem @{" Fenster «Einstellungen - System» öffnen " rxs "address ignition.1 prefs system req"}
@endnode


@node menu_edit "ignition - Menüs: Bearbeiten"
 @{b}2.2. Menüs@{ub}

 @{b}Bearbeiten@{ub}
 @{" Ausschneiden          (X)" link item_cut}
 @{" Kopieren              (C)" link item_copy}
 @{" Einfügen              (V)" link item_paste}
 @{" Duplizieren              " link item_duplicate}

 @{" Zwischenablage           " link window_clip}

 @{" Zellen einfügen        » " link menu_insertcells}
 @{" Zellen entfernen       » " link menu_removecells}

 @{" Text löschen        (Del)" link item_delete_text}
 @{" Löschen                  " link item_delete}

 @{" Suchen                (F)" link window_find}
 @{" Ersetzen              (R)" link window_replace}
 @{" Nächstes              (W)" link item_find_next}

 @{" Rückgängig            (U)" link item_undo}
 @{" Wiederholen           (R)" link item_redo}

@endnode


@node item_cut "ignition - Menüs: Bearbeiten/Ausschneiden"
Entfernt das aktuelle Objekt (oder Zellen) in die Zwischenablage.
@endnode


@node item_copy "ignition - Menüs: Bearbeiten/Kopieren"
Kopiert das aktuelle Objekt (oder Zellen) in die Zwischenablage.
@endnode


@node item_pase "ignition - Menüs: Bearbeiten/Einfügen"
Fügt das momentan in der Zwischenablage befindliche Objekt (oder Zellen) in die Tabelle ein.
Zellen werden an der aktuellen Position eingefügt, Objekte an den Koordinaten, an denen sie in die Zwschenablage kopiert/verschoben worden waren (oder versetzt davon, wenn mehrfach eingefügt wird).
@endnode


@node item_duplicate "ignition - Menüs: Bearbeiten/Duplizieren"
Dupliziert das aktuell ausgewählte Objekt und fügt es leicht versetzt in die aktuelle Seite ein.
Diese Funktion ist nicht auf Zellen anwendbar.
@endnode


@node item_delete_text "ignition - Menüs: Bearbeiten/Text löschen"
Löscht den Text der aktuellen Zelle(n).
@endnode


@node item_delete "ignition - Menüs: Bearbeiten/Löschen"
Löscht die angewählten Objekte oder Zellen.
@endnode


@node item_item_find_next "ignition - Menüs: Bearbeiten/Nächstes"
Falls nach einer Zelle gesucht wurde, oder falls Zelleigenschaften nach einem definierten Muster ersetzt werden sollen, springt diese Funktion zur nächsten passenden Zelle weiter.

@{u}    @{uu}

@{" Fenster «Suchen» öffnen " rxs "address ignition.1 search req"}
@{" Fenster «Suchen und Ersetzen» öffnen " rxs "address ignition.1 replace req"}
@endnode


@node item_undo "ignition - Menüs: Bearbeiten/Undo"
Damit können alle Änderungen an der Tabelle rückgängig gemacht werden. Mit @{" Redo " link item_redo} können diese wieder hergestellt werden.
@endnode


@node item_redo "ignition - Menüs: Bearbeiten/Redo"
Damit können alle Änderungen an der Tabelle wieder hergestellt werden, die zuvor mit @{" Undo " link item_undo} rückgängig gemacht wurden.
@endnode


@node menu_view "ignition - Menüs: Ansicht"
 @{b}2.2. Menüs@{ub}

 @{b}Ansicht@{ub}
 @{" 50%                      " link item_zoom}
 @{" 100%                     " link item_zoom}
 @{" 200%                     " link item_zoom}
 @{" 400%                     " link item_zoom}
 @{" 800%                     " link item_zoom}

 @{" Übersicht                " link item_zoom}
 @{" Andere...                " link window_zoom}

@endnode


@node item_zoom "ignition - Menüs: Ansicht"
Hier können Sie eine bestimmte Vergrößerung für die aktuelle Tabelle wählen.
Der Menüpunkt "Übersicht" versucht, die Tabelle komplett darzustellen.
@endnode


@node menu_object "ignition - Menüs: Objekt"
 @{b}2.2. Menüs@{ub}

 @{b}Objekt@{ub}
 @{" Zelle                  » " link menu_cell}
 @{" Tabelle                » " link menu_page}
 @{" Namen...                 " link window_prefs_names}
#ifndef	IGNITION_LITE_EDITION
 @{" Datenbank              » " link menu_db}
#endif

 @{" Dokument...              " link window_document}
 @{" Skripte...               " link window_scripts}

 @{" Diagram erstellen...     " link window_diagram}

 @{" Information...           " link window_object}

 @{" Objektebene            » " link menu_objdepth}

@rem @{" Gruppieren               " link unknown}
@rem @{" Gruppierung lösen        " link unknown}
@rem
 @{" Einfügen                 " link window_insert_object}

@endnode


@node menu_preferences "ignition - Menüs: Einstellungen"
 @{b}2.2. Menüs@{ub}

 @{b}Einstellungen@{ub}
 @{" ändern...                " link window_prefs}

 @{" laden...                 " link item_load_prefs}

 @{" speichern                " link item_save_prefs}
 @{" speichern als...         " link item_save_prefs_as}


@endnode


@node item_load_prefs "ignition - Menüs: Einstellungen/laden..."
Ermöglicht die Auswahl von vorher gespeicherten Einstellungen über ein Dateiauswahlfenster.
@endnode


@node item_save_prefs "ignition - Menüs: Einstellungen/speichern"
Speichert die aktuellen Einstellungen als Standardeinstellungen (üblicherweise als "config/ignition.prefs").
@endnode


@node item_save_prefs_as "ignition - Menüs: Einstellungen/speichern..."
Ermöglicht die Auswahl eines alternativen Namens und Ortes über einen Dateiauswahlfenster, an dem die aktuellen Einstellungen gespeichert werden sollen.
@endnode


@node menu_insertcells "ignition - Menüs: Bearbeiten/Zellen einfügen"
 @{b}2.2. Menüs: Bearbeiten@{ub}

 @{b}Zellen einfügen@{ub}
 @{" Spalte                   " link item_insert_column}
 @{" Reihe                    " link item_insert_row}

 @{" nach rechts              " link item_insert_to_right}
 @{" nach unten               " link item_insert_to_bottom}


@endnode


@node item_insert_column "ignition - Menüs: Zellen einfügen/Spalte"
Fügt eine Spalte in die Tabelle an der aktuellen Position ein.
@endnode


@node item_insert_row "ignition - Menüs: Zellen einfügen/Reihe"
Fügt eine Reihe in die Tabelle an der aktuellen Position ein.
@endnode


@node item_insert_to_right "ignition - Menüs: Zellen einfügen/nach rechts"
Fügt für die ausgewählten Zellen neue ein und verschiebt die vorhandenen nach rechts.
@endnode


@node item_insert_to_bottom "ignition - Menüs: Zellen einfügen/nach unten"
Fügt für die ausgewählten Zellen neue ein und verschiebt die vorhandenen nach unten.
@endnode


@node menu_removecells "ignition - Menüs: Bearbeiten/Zellen entfernen"
 @{b}2.2. Menüs: Bearbeiten@{ub}

 @{b}Zellen entfernen@{ub}
 @{" Spalte                   " link item_remove_column}
 @{" Reihe                    " link item_remove_row}

 @{" nach links               " link item_remove_to_left}
 @{" nach oben                " link item_remove_to_top}


@endnode


@node item_remove_column "ignition - Menüs: Zellen entfernen/Spalte"
Entfernt die aktuelle Spalte aus der Tabelle.
@endnode


@node item_remove_row "ignition - Menüs: Zellen entfernen/Reihe"
Entfernt die aktuelle Reihe aus der Tabelle.
@endnode


@node item_remove_to_left "ignition - Menüs: Zellen entfernen/nach links"
Entfernt alle ausgewählten Zellen und läßt die vorhandenen von rechts nach links nachrücken.
@endnode


@node item_remove_to_top "ignition - Menüs: Zellen entfernen/nach oben"
Entfernt alle ausgewählten Zellen und läßt die vorhandenen von unten nach oben nachrücken.
@endnode


@node menu_cell "ignition - Menüs: Objekt/Zelle"
 @{b}2.2. Menüs: Objekt@{ub}

 @{b}Zelle@{ub}
 @{" Eigenschaften...         " link window_cell}
 @{" Rahmen...                " link window_frame}
 @{" Notiz...                 " link window_notes}

 @{" Größe...                 " link window_cell_size}
 @{" optimale Breite          " link item_optimal_width}
 @{" optimale Höhe            " link item_optimal_height}


@endnode


@node item_optimal_width "ignition - Menüs: Zelle/optimale Breite"
Paßt die Breite der aktuellen Spalte so an, daß alle Zellen komplett enthalten sind, d.h. setzt die Breite auf die Breite der breitesten Zelle.
@endnode


@node item_optimal_height "ignition - Menüs: Zelle/optimale Höhe"
Paßt die Höhe der aktuellen Reihe so an, daß alle Zellen komplett enthalten sind, d.h. setzt die Höhe auf die Höhe der höchsten Zelle (nach Schriftgröße).
@endnode


@node menu_page "ignition - Menüs: Objekt/Tabelle"
 @{b}2.2. Menüs: Objekt@{ub}

 @{b}Tabelle@{ub}
 @{" Nächste                  " link item_next_page}
 @{" Vorherige                " link item_prev_page}
 ~~
 @{" Neu                      " link item_new_page}
 @{" löschen                  " link item_delete_page}
 @{" nach oben                " link item_page_to_top}
 @{" nach unten               " link item_page_to_bottom}
 ~~
 @{" Eigenschaften...         " link window_page}


@endnode


@node item_next_page "ignition - Menüs: Tabelle/Nächste"
Geht im aktuellen Dokument zur nächsten Seite (Tabelle).
@endnode


@node item_prev_page "ignition - Menüs: Tabelle/Vorherige"
Geht im aktuellen Dokument zur vorherigen Seite (Tabelle).
@endnode


@node item_new_page "ignition - Menüs: Tabelle/Neu"
Fügt eine neue Seite (Tabelle) am Ende des Dokumentes ein, und macht sie zur aktuellen.
@endnode


@node item_delete_page "ignition - Menüs: Tabelle/löschen"
Löscht die aktuelle Seite (Tabelle) aus dem Dokument.
@endnode


@node item_page_to_top "ignition - Menüs: Tabelle/nach oben"
Verschiebt die aktuelle Seite (Tabelle) im Dokument eine Seite nach oben, falls möglich.
@endnode


@node item_page_to_bottom "ignition - Menüs: Tabelle/nach unten"
Verschiebt die aktuelle Seite (Tabelle) im Dokument eine Seite nach unten, falls möglich.
@endnode


#ifndef IGNITION_LITE_EDITION
@node menu_db "ignition - Menüs: Objekt/Datenbank"
 @{b}2.2. Menüs: Objekt@{ub}

 @{b}Datenbank@{ub}
 @{" definieren...            " link window_database}
 ~~
 @{" Index...                 " link window_database_index}
 @{" Filter...                " link window_database_filter}
 @{" Maske...                 " link window_database_mask}
 ~~
 @{" Eintrag hinzufügen       " link item_database_add_record}
 @{" Suche (wechseln)         " link item_database_search}


@endnode


@node item_database_add_record "ignition - Menüs: Datenbank/Eintrag hinzufügen"
Fügt in der aktuellen Datenbank einen Eintrag hinzu. Es wird versucht, diese dabei anhand der aktuellen Zellposition zu ermitteln; falls die Zelle innerhalb einer Datenbankdefinition oder Maske liegt, kann die Datenbank bestimmt werden.
Falls die Datenbank Referenzen zu anderen beinhaltet, werden in diesen Datenbanken automatisch ebenfalls neue Einträge hinzugefügt.
@endnode


@node item_database_search "ignition - Menüs: Datenbank/Suche (wechseln)"
Dieser Menüpunkt schaltet die aktuelle Maske in den Suchmodus. Werden in allen Feldern Eingaben gemacht (oder der Menüpunkt mit teilweisen Eingaben erneut ausgewählt), wird ein Filter generiert, der nur die der Suchanfrage entsprechenden Einträgen anzeigt.
Bei nochmaliger Auswahl des Menüpunktes wird der Filter wieder ausgeschaltet.
Ein ähnlicher Filter kann auch über das @{" Filter erstellen " link window_database_filter} Fenster definiert werden.

@{u}    @{uu}

@{" Fenster «Filter erstellen» öffnen " rxs "address ignition.1 filter req"}
@endnode
#endif	/* IGNITION_LITE_EDITION */
								
 
@node menu_objdepth "ignition - Menüs: Objektebene"
 @{b}2.2. Menüs@{ub}

 @{b}Objektebene@{ub}
 @{" nach vorne               " link item_object_to_front}
 @{" nach hinten              " link item_object_to_back}


@endnode


@node item_object_to_front "ignition - Menüs: Objektebene/nach vorne"
Damit kann das aktuell ausgewählte Objekt eine Ebene nach vorne verschoben werden.
Wird es also vorher teilweise von einem anderen Objekt verdeckt, kann man dies hiermit ändern.
@endnode


@node item_object_to_back "ignition - Menüs: Objektebene/nach hinten"
Damit kann das aktuell ausgewählte Objekt eine Ebene nach hinten verschoben werden.
Falls es also vor einem anderen Objekt liegt, wird es nachher von diesem verdeckt.
@endnode


***********************************************************************************
* 2.3. Piktogrammleiste                                                           *
***********************************************************************************

@node iconbar "ignition - Piktogrammleiste"
 @{b}2.3. Piktogrammleiste@{ub}

In der Standardkonfiguration finden Sie der Reihe nach die folgenden Knöpfe in der Piktogrammleiste:

 @{" Einstellungen: Darstellung " link window_prefs_display}
 @{" Einstellungen: Palette     " link window_prefs_palette}
 @{" Aufsteigend sortieren      " link icon_sort_ascending}
 @{" Absteigend sortieren       " link icon_sort_descending}
 @{" Zellbreite setzen          " link icon_set_cell_width}

 @{" Zelle(n) umrahmen          " link window_frame}
 @{" Nachkommastelle weniger    " link icon_less_decimal_places}
 @{" Nachkommastelle mehr       " link icon_more_decimal_places}
 @{" Gruppentrennzeichen        " link icon_separate_thousands}
 @{" Negative Werte klammern    " link icon_neg_values}

 @{" Undo                       " link icon_undo}
 @{" Redo                       " link icon_redo}

#ifndef IGNITION_LITE_EDITION
 @{" Modus wechseln             " link icon_interactive_mode}
#endif
  
Die Piktogrammleiste kann unter @{" Einstellungen - Darstellung " link window_prefs_display} ein- und ausgeschaltet werden.
Zusätzlich kann sie projektabhängig über das Fenster @{" Einstellungen - Piktogrammleiste " link window_prefs_iconbar} konfiguriert werden.

@{u}    @{uu}

@{" Fenster «Einstellungen - Darstellung» öffnen " rxs "address ignition.1 prefs display req"}
@{" Fenster «Einstellungen - Piktogrammleiste» öffnen " rxs "address ignition.1 prefs icon req"}
@endnode


@node icon_undo "ignition - Undo"
Damit können alle Änderungen an der Tabelle rückgängig gemacht werden. Mit @{" Redo " link icon_redo} können diese wieder hergestellt werden.

In der Hilfeleiste wird jeweils die aktuelle Änderung angezeigt, wenn der Mauszeiger über diesem Knopf ist.
@endnode


@node icon_redo "ignition - Redo"
Damit können alle Änderungen an der Tabelle wieder hergestellt werden, die zuvor mit @{" Undo " link icon_undo} rückgängig gemacht wurden.

In der Hilfeleiste wird jeweils die wieder herzustellende Änderung angezeigt, wenn der Mauszeiger über diesem Knopf ist.
@endnode


@node icon_sort_descending "ignition - Absteigend sortieren"
Dieser Knopf veranlaßt eine absteigende Sortierung des ausgewählten Blockes.
Wird mehr als eine Spalte angewählt, wird nach der ersten sortiert, und die Reihenfolge der Zeilen der anderen entsprechend geändert.
@endnode


@node icon_sort_ascending "ignition - Aufsteigend sortieren"
Dieser Knopf veranlaßt eine aufsteigende Sortierung des ausgewählten Blockes.
Wird mehr als eine Spalte angewählt, wird nach der ersten sortiert, und die Reihenfolge der Zeilen der anderen entsprechend geändert.
@endnode


@node icon_set_cell_width "ignition - Zellbreite setzen"
Dieser Knopf setzt die Breite (oder eher Spannweite) der aktuellen Zelle auf die momentan selektierte Breite.
Das kann zum Beispiel dazu genutzt werden, Überschriften über eine Tabelle zu zentrieren - geben Sie die Überschrift zunächst linksbündig über der Tabelle ein, wählen dann diese Zelle und alle folgenden über die gesamte Breite der Tabelle an, und setzen mit diesem Knopf die Zellbreite.
Wird nun die erste Zelle mit der Überschrift ausgewählt, spannt der Rahmen, der die aktuelle Zelle hervorhebt über die ganze Tabellenbreite. Wenn Sie die Zelle nun zentrieren, wird die Überschrift auch über die gesamte Tabelle zentriert.

@{u}    @{uu}

@{" Aktuelle Zelle zentrieren " rxs "address ignition.1 align center"}
@endnode


@node icon_less_decimal_places "ignition - Nachkommastellenanzahl verringern"
Verringert die Anzahl der Nachkommastellen.
Die kleinste Stufe ist allerdings nicht "0 Stellen", wie man erwarten könnte, sondern die "Auto"-Einstellung, d.h. es werden so viele Stellen angezeigt wie nötig, um den Wert zu repräsentieren - Nullen am Ende werden also weggelassen.
Maximal werden aber immer nur 10 Nachkommastellen angezeigt.
@endnode


@node icon_more_decimal_places "ignition - Nachkommastellenanzahl erhöhen"
Erhöht die Anzahl der Nachkommastellen.
Maximal werden 10 Nachkommastellen angezeigt.
Die kleinste Stufe ist allerdings nicht "0 Stellen", wie man erwarten könnte, sondern die "Auto"-Einstellung, d.h. es werden so viele Stellen angezeigt wie nötig (maximal jedoch 10), um den Wert zu repräsentieren - Nullen am Ende werden also weggelassen.
@endnode


@node icon_separate_thousands "ignition - Gruppentrennzeichen wechseln"
Wechselt das Gruppentrennzeichen - Tausender werden entweder gesondert geschrieben oder nicht, also 1 Million entweder als 1000000 oder als 1.000.000 (je nach eingesteller Sprache kann das z.B. auch 1,000,000 sein).
@endnode


@node icon_neg_values "ignition - Negative Werte klammern"
Negative Werte werden geklammert oder normal mit Minuszeichen dargestellt, also z.B. -58 == (58).
@endnode


@node icon_interactive_mode "ignition - Modus wechseln"
Dieser Knopf wechselt in den @{" interaktiven Modus " link interactive_mode} und zurück.
Der aktuelle Modus, in dem sich das Dokument befindet wird in der @{" Hilfsleiste " link helpbar} angezeigt.
@endnode


***********************************************************************************
* 2.4. Werkzeugleiste                                                             *
***********************************************************************************

@node toolbar "ignition - Werkzeugleiste"
 @{b}2.4. Werkzeugleiste@{ub}

Die Werkzeugleiste hat der Reihe nach die folgenden Elemente:

 @{" Schrift               " link proj_gadget046}
 @{" Schriftgröße          " link proj_gadget047}
 @{" Stil - Normal         " link proj_gadget051}
 @{" Stil - Fett           " link proj_gadget052}
 @{" Stil - Kursiv         " link proj_gadget053}
 @{" Stil - Unterstrichen  " link proj_gadget054}
 @{" Vordergrundfarbe      " link proj_gadget055}
 @{" Hintergrundfarbe      " link proj_gadget056}
 @{" links ausrichten      " link proj_gadget057}
 @{" rechts ausrichten     " link proj_gadget058}
 @{" zentrieren            " link proj_gadget059}
 @{" hoch                  " link proj_gadget061}
 @{" mittel                " link proj_gadget062}
 @{" tief                  " link proj_gadget063}
 @{" Vergrößerung          " link proj_gadget048}

Die Werkzeugleiste kann unter @{" Einstellungen - Darstellung " link window_prefs_display} ein- und ausgeschaltet werden.

@{u}    @{uu}

@{" Fenster «Einstellungen - Darstellung» öffnen " rxs "address ignition.1 prefs display req"}
@endnode


***********************************************************************************
* 2.5. Formelleiste                                                               *
***********************************************************************************

@node formbar "ignition - Formelleiste"
 @{b}2.5. Formelleiste@{ub}

Die Formelleiste besteht aus folgenden Elementen:

 @{" Positionsanzeige       " link form_pos}
 @{" Eingabefeld            " link proj_gadget042}
 @{" schnelle Formelauswahl " link proj_gadget041}
 @{" Formelauswahl          " link proj_gadget043}
 @{" Seitenanzeige          " link proj_gadget039}

Die Formelleiste kann unter @{"Einstellungen - Darstellung" link window_prefs_display} ein- und ausgeschaltet werden.

@{u}    @{uu}

@{" Fenster «Einstellungen - Darstellung» öffnen " rxs "address ignition.1 prefs display req"}
@endnode


@node form_pos "ignition - Formelleiste/Positionsanzeige"
@toc formbar
In diesem Feld wird die aktuelle Position in der Tabelle oder die Position des aktuell markierten Blockes gezeigt.

Zusätzlich können Sie sich die Summe oder den Mittelwert der markierten Zellen anzeigen lassen. Dies können Sie im Fenster @{" Einstellungen - Tabelle " link window_prefs_table} unter «Diverses/Blockanzeige» auswählen.

@{u}    @{uu}

@{" Fenster «Einstellungen - Tabelle» öffnen " rxs "address ignition.1 prefs table req"}
@endnode


***********************************************************************************
* 2.6. Hilfeleiste                                                                *
***********************************************************************************

@node helpbar "ignition - Hilfeleiste"
@{b}2.6. Hilfeleiste@{ub}

In dieser Leiste werden Statusmeldungen und Hilfetexte und Zellnotizen angezeigt.
Sie kann unter @{" Einstellungen - Darstellung " link window_prefs_display} ein- und ausgeschaltet werden.

Am rechten Rand werden ständige Status-Informationen angezeigt:
 @{b}inter@{ub} - @{" interaktiver Modus " link mode_interactive}
 @{b}edit@{ub} - @{" Editier-Modus " link mode_edit}

Im interaktiven Modus lösen Objekte wie Eingabefelder und Knöpfe ihre zugewiesene Aktion aus und können benutzt werden.
Im Editier-Modus können solche Objekte hinzugefügt und verändert werden.

@{u}    @{uu}

@{" In den interaktiven Modus wechseln " rxs "address ignition.1 interactive set"} @{" In den Editier-Modus wechseln " rxs "address ignition.1 interactive none"}
@{" Fenster «Einstellungen - Darstellung» öffnen " rxs "address ignition.1 prefs display req"}

@endnode


@node mode_interactive "ignition - Interaktiver Modus"
In diesem Modus können die Objekten zugewiesenen Aktionen ausgeführt werden, definierte Ereignisse werden ausgeführt.
Allerdings kann der Inhalt der Tabelle nur sehr eingeschränkt geändert werden.

Siehe auch @{"Editier-Modus" link mode_edit}.

@{u}    @{uu}

@{" In den interaktiven Modus wechseln " rxs "address ignition.1 interactive set"} @{" In den Editier-Modus wechseln " rxs "address ignition.1 interactive none"}

@endnode

 
@node mode_edit "ignition - Editier-Modus"
In diesem Modus stehen alle Editier-Funktionen der Tabelle zur Verfügung. Objekte können hinzugefügt und geändert werden; deren Aktionen funktionieren jedoch nicht. D.h. wenn Sie auf einen Knopf drücken wählen Sie in aus, führen aber nicht das ihm zugewiesene Kommando aus.

Siehe auch @{"Interaktiver Modus" link mode_interactive}.

@{u}    @{uu}

@{" In den interaktiven Modus wechseln " rxs "address ignition.1 interactive set"} @{" In den Editier-Modus wechseln " rxs "address ignition.1 interactive none"}

@endnode

 
***********************************************************************************
* 4.Anhang                                                                        *
***********************************************************************************

@node faq "ignition - Fragen und Antworten"
 @{b}3.1. Fragen & Antworten@{ub}

(noch nicht verfügbar)
@endnode


@node troubleshooting "ignition - Fehlersuche"
 @{b}3.2. Fehlersuche@{ub}

 @{b}1. Beim Speichern öffnet sich nicht die Dateityp-Auswahl, obwohl der Befehl sie eigentlich öffnen sollte@{ub}

Die Dateityp-Auswahl basiert auf Funktionen der standard ASL-Requester - einige ASL-Ersatz-Requester implementieren diese jedoch nicht.
Eine Lösung bietet auf jeden Fall das Zurückschalten auf die originalen Requester.

@endnode


@node support "ignition - Kontaktadressen für Support"
 @{b}3.3. Support-Adresse@{ub}

Für Fragen, Probleme zur aktuellen oder Ideen für zukünftige Versionen der Software, setzen Sie sich bitte direkt mit pinc Software in Verbindung.

eMail: support@pinc-software.de

Post:  pinc Software
       Axel Dörfler

       Lange Straße 4a
       49080 Osnabrück

@endnode


***********************************************************************************
* Index                                                                           *
***********************************************************************************

@node index "ignition - Index"
Index noch nicht erstellt.
@endnode
